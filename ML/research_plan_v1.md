# 坡地地震动放大效应 × 机器学习 — 分析与研究规划报告 v1

> **定位**：硕士学位论文规划（兼顾可拆分期刊论文）。算力约束：个人电脑、夜间批量跑 Abaqus（每月可新增约几十至上百个工况）。
> **依据**：本报告所有 v3 成果数字均取自 `ML/outputs_v3/` 下的实测 JSON（`train_metrics.json`、`evaluate_metrics.json`、`baseline_metrics.json`、`diagnostics_v3.json`），可复现，非估计值。
> **生成日期**：2026-06-10

---

## 第一部分 v3 训练成果分析：值多少、缺什么

### 1.1 成果总览（5 折 GroupKFold，TAF_h 通道）

| 模型/基线 | 全区 MAE | 全区 R² | 峰值区(x/h≤2) R² | 结论 |
|------|------|------|------|------|
| **v3 GPR**（主力） | **0.0244** | **0.947** | **0.880** | 当前最优 |
| v3 XGBoost | 0.0330 | 0.920 | 0.793 | 次优，提供特征重要性 |
| B2 每地形取 3 波平均（oracle 基线） | 0.0469 | 0.829 | 0.519 | 被 GPR 大幅超越 |
| B3 规则网格线性插值（不用 ML 的下限） | 0.0708 | 0.633 | 0.221 | 证明 ML 有真实增益 |
| B1 全局均值 | 0.1030 | 0.321 | 0.082 | R² 零点参照 |
| v2 最强深度模型 DeepONet | 0.0506 | ~0.835 | — | 已被全面超越 |

四通道完整评估（`evaluate_metrics.json`）：PGA_h R²=0.948、PGA_v R²=0.977、TAF_h R²=0.946；TAF_v 仅在"可靠点"（98.85% 的点）上评估，R²=0.861 但 MAE=0.547（量纲大、依旧病态）。

**三个可以直接写进论文的硬结论**（均已实测证实）：

1. **小数据场景下"物理特征 + 经典 ML"碾压端到端深度学习**：GPR MAE 0.0244 vs 四个深度模型 0.0506~0.0572，且训练成本 CPU 秒级 vs GPU 小时级。四个原理迥异的深度模型成绩挤在 0.79~0.84，证明瓶颈在问题定义而非模型容量——这本身就是有方法论价值的负结果。
2. **波特征确有可学的信号**：GPR（0.0244）显著低于"完全无视波"的 B2 oracle 基线（0.0469），说明反应谱形状特征在"见过的波"范围内贡献了约一半的误差下降。
3. **地形主导的定量证据**：跨地形标准差 0.129 是跨波 0.052 的 2.47 倍（174 工况全量实测）。

### 1.2 四个关键短板（创新点全部从这里生长）

**短板 S1：波外推彻底失败——这是当前框架的天花板。**
留一波测试：GPR MAE=0.107、R²=0.27，峰值区 R²≈0.004，远场区 R² 为负。注意 0.107 几乎等于全局均值基线 B1 的 0.103——**换一条没见过的波，模型退化到"闭眼猜平均"**。根因不是模型差，而是训练集只有 3 条波，波特征空间只有 3 个点，任何插值/外推都无从谈起。

**短板 S2：几何边界外推失败。**
留一高度测试（全区 R²）：h=10 → **-6.83**；h=50 → 0.77；h=100 → 0.84；h=200 → 0.58；h=400 → **0.02**。内插可用、两端外推崩溃，这是 GPR/树模型的本质属性。留一角度则全部良好（R² 0.85~0.96，因为入射角有 7 个水平、网格密）。**深层原因**：模型看到的是绝对量 h，而控制物理的其实是无量纲量（高度/波长比），换 h 等于换了它没见过的物理区间。

**短板 S3：TAF_v 定义病态尚未根治。**
TAF_v 原始最大值达 1.77×10¹¹（平地竖向 PGA 近零导致除法爆炸），1.15% 的点 flat |PGA_v|<1e-3，6.5% 的点 TAF_v>10。当前 clip(0,10)+排除近零点只是止血，不是治疗。

**短板 S4：物理覆盖窄。**
当前 FEM（`VAB_oblique_v3.py`）是均质、线弹性、单一地层的二维平面应变模型。真实坡地的放大来自**地形效应 × 地层（共振）效应的耦合**，单层模型天然缺失了阻抗比、覆盖层厚度、场地卓越周期这些一级控制变量——这正是你下一步多层模型要补的，也是论文升维的关键。

---

## 第二部分 创新性问题：怎么让"ML 预测放大效应"立得住

### 2.1 文献现状：哪些已经被做掉了

检索结论（2024–2026 年文献）：

- "用 ML（XGB/RF/ANN/GPR）预测坡地地形放大系数"的**框架性论文已经存在**（如 2024 年 *Engineering Applications of AI* 的坡地地形放大预测框架），输入坡高、坡角、波频率，输出放大比。**所以"我用了 ML 预测 TAF"本身不构成创新**。
- 场地放大的 ML 预测、迁移学习做场地响应（2025）也已有大量工作，但多针对一维水平场地、预测标量放大因子。
- **地形×地层耦合放大**是活跃前沿（2014 Brennan 等 2D 线性/等效线性分析；2025 *Soil Dynamics and Earthquake Engineering* 刚发表了"层状岩坡在垂直/斜入射波下的地形-地层组合放大"），但**这些工作是参数分析性质的，尚未见"耦合放大的 ML 代理模型 + 不确定性量化"**。

### 2.2 你的创新点应该怎么立：六个候选点（按推荐优先级排序）

创新的总原则：**不要把"用了什么模型"当创新，要把"解决了哪个别人没解决的具体困难"当创新**。你的四个短板，每一个的解法都是一个潜在创新点。

#### C1（强烈推荐，核心创新）：目标从 PGA 域转到频域——预测"谱放大函数"，根治三波困境

**机理**：你的 FEM 是线弹性系统。线性系统的本质属性是：**地表任意点的传递函数 H(f, x/h) 与输入波完全无关**，只由几何+地层+入射角决定。而 TAF（基于 PGA 的比值）是 H(f) 与输入波频谱卷积后的派生量，所以才"波相关"。当前框架费力学习的"波特征 → TAF 修正"，其实是在用 ML 模拟一次卷积——这是绕远路。

**做法**：把 ML 的目标改为 H(f, x/h)（或谱放大曲面 Sa比(T, x/h)）。预测任意新波的响应時：`新波加速度 FFT × 预测的 H(f) → IFFT`，理论上**对任意波都成立**，留一波问题直接消失（线弹性范围内）。

**为什么这是高质量创新**：(a) 它不是换模型，而是用物理结构重写问题定义，审稿人无法用"换个网络也行"来削弱它；(b) 它把代理模型从"只能算 3 条波"升级为"任意输入波的虚拟传感器"，工程价值跃升；(c) 文献中坡地 ML 预测均以标量/PGA 放大为目标，以全场传递函数曲面为目标 + POD 压缩二维场的做法未见先例。

**数据成本极低（这是关键）**：线弹性下，一次**宽频脉冲（Ricker 子波或白噪声）输入**即可一次性提取整个 H(f, x/h) 曲面——每个几何工况只需 1 次计算，174 个现有几何 ≈ 1~2 个月夜间批量即可全部覆盖。另外：3 条已有波若保留了地表加速度时程（查 ODB），可直接反演出 3 个独立的 H 估计，互相印证（线弹性下三者应一致，其离散度就是数值噪声的免费估计）。

#### C2（强烈推荐，与 C1 配套）：无量纲特征体系，根治高度外推失败

把输入特征从绝对量改为控制物理的无量纲量：

| 现特征 | 替换为 | 物理含义 |
|------|------|------|
| h（绝对坡高） | η = h/λ = h·f/Vs | 高度/波长比——地形放大的第一控制量（Geli/Ashford 经典结论） |
| 波特征 Tp、Sa(T) | Tp/Ts、Sa(Ts) | 输入卓越周期 / 场地固有周期之比（共振判据） |
| 多层参数（未来） | 阻抗比 α=ρ₁Vs₁/ρ₂Vs₂、厚度比 d/h、Ts=4Σ(dᵢ/Vsᵢ) | 地层共振控制量 |

**预期收益**：h=10 与 h=400 在 η 空间里不再是"两个没见过的端点"，而是同一物理规律的不同采样点。留一高度的 R² 从 -6.8/0.02 提升到正值区间，本身就是论文里非常有说服力的一张对比图（"物理无量纲化使外推成为可能"）。同时它让你的模型具备**跨尺度推广能力**的论述基础。

#### C3（核心创新，论文主轴）：地形×地层耦合放大的"残差学习"框架

多层模型上线后，不要让 ML 直接学总放大，而是做**物理分解**：

```
FEM 总放大 A_total(f, x/h)
   = A_1D(f)            ← 水平层状场地的一维理论解（Thomson-Haskell 传递矩阵，毫秒级可算）
   × A_topo(f, x/h)      ← 纯地形项（v3 已有的均质坡地结果）
   × R_coupling(f, x/h)  ← 耦合残差 = FEM ÷ (一维理论 × 纯地形)，让 ML 只学这一项
```

**三重好处**：(a) ML 的学习负担骤降——前两项是免费的物理/已有成果，残差量级小、变化平缓，小样本就能学好；(b) R_coupling 本身就是**科学发现**：它直接量化了"规范把地形系数和场地系数简单相乘"到底错多少（Eurocode 8 的 ST 与场地系数 S 是独立相乘的，中国 GB 50011 的局部突出地形放大系数同样不考虑耦合）——R 显著偏离 1 的参数区域就是规范不安全/过保守的区域，这是有直接工程价值的结论；(c) 该分解天然继承 v3 成果（A_topo 就是 v3 的模型），论文前后章节形成闭环而不是推倒重来。

#### C4（已具雏形，做深即可）：不确定性量化 + 适用域判别

GPR 的 σ 输出目前只画了图。做深一步：(a) **校准**——检验预测区间的名义覆盖率 vs 实际覆盖率（可靠性图）；(b) **适用域判别器**——给任意新输入算一个"可信/外推预警"标签（基于 GPR σ 阈值 + 训练集凸包距离），并在工具输出中强制显示。文献中坡地 ML 预测几乎都不带校准过的 UQ，这一点做扎实就是差异化。

#### C5（算力受限者的特色创新）：GPR 方差驱动的主动学习采样闭环

个人电脑跑不了全因子扫描，这个约束可以转化为方法贡献：

```
循环：用当前 GPR 在候选参数空间算预测方差 σ²(x)
      → 选 σ² 最大（或 σ²×物理重要性加权）的前 N 个工况
      → 夜间批量跑 Abaqus → 加入训练集 → 重训
停止：验证集 MAE 收敛 或 算力预算用尽
```

论文表述："以 30% 的计算预算达到全因子扫描 95% 的精度"——用对比实验（主动学习 vs 随机采样 vs 规则网格，同预算下精度曲线）支撑。这类"仿真预算受限下的代理建模工作流"在岩土地震工程领域很少有人做扎实。

#### C6（已完成，直接收割）：小数据方法论对照研究

v2 vs v3 的完整对照（4 个深度模型 × 5 折 + 经典 ML + 3 个基线 + 诚实的留一外推）已经构成一篇完整的方法论论文：**"数据稀缺的 FEM 代理建模中，物理特征工程 + 经典 ML 优于端到端深度学习"**。建议尽快成文投出（EI/SCI 四区起步均可），作为硕士期间第一篇产出，不要等多层模型。

### 2.3 创新点 → 论文结构映射

| 创新点 | 学位论文章节 | 可拆期刊论文 | 状态 |
|------|------|------|------|
| C6 经典 ML vs DL 对照 | 第 3 章（方法选型） | 论文① 方法论文 | **数据已齐，可动笔** |
| C1 频域目标 + C2 无量纲化 | 第 4 章（均质坡地代理模型） | 论文②（核心，目标 SDEE/SoilDyn 级） | 待实施，约 3 个月 |
| 多层 FEM + 参数分析 | 第 5 章 | （并入论文③） | 待实施 |
| C3 耦合残差学习 + C4 UQ + 规范对比 | 第 6 章（论文重心） | 论文③（核心） | 待实施 |
| C5 主动学习 | 第 5/6 章方法小节 | （并入论文③或独立短文） | 待实施 |

---

## 第三部分 推广到多层地层模型：调整清单

### 3.1 FEM 侧调整（按必要性排序）

**A1. 粘弹性人工边界系数逐节点取局部材料参数（必须）。**
当前 `VAB_oblique` 用全局统一的 cs、vv、density 算弹簧-阻尼系数。多层模型中，边界节点落在哪一层，K、C 就必须用**该层**的 G、cp、cs（侧边界穿越多层时尤其重要）。脚本改动：建立"节点 y 坐标 → 层号 → 材料参数"查询函数，逐节点赋 K、C。

**A2. 斜入射自由场输入重新推导（必须，工作量最大的力学环节）。**
等效节点力依赖边界处的自由场应力+速度。均质半空间有闭式解；**层状半空间斜入射 SV 波的自由场必须用传递矩阵法（Thomson-Haskell）在频域逐层求解**，再 IFFT 回时域得到每个边界节点的时程。实施建议：写一个独立的自包含 Python 模块（按项目规范内联进主脚本）`层状自由场求解器`，输入层参数+入射角+波时程，输出任意深度的应力/速度时程；先用均质退化情形与现有 v3 闭式解对拍（误差应 <1%）。
⚠️ 注意**临界角**：SV 波斜入射在 Vp/Vs 决定的临界角（泊松比 0.3 时约 32°）以上会产生非均匀波，公式体系不同。你现有入射角网格最大 30°，建议多层版本仍以 30° 为上限并在论文中说明。

**A3. 网格尺寸按最软层控制（必须）。**
单元尺寸 ≤ Vs_min/(8~10·f_max)。若表层软土 Vs=150 m/s、关心频率到 10 Hz，则单元 ≤ 1.5~1.9 m——比现在均质模型细很多，单算例耗时会上升，影响夜间批量产能，需在参数设计时就把"软层 Vs 下限"和"截止频率"定死。

**A4. 阻尼分层赋值（必须）。**
线弹性+人工边界仍需材料阻尼避免高频污染。建议各层 Rayleigh 阻尼按该层主导频率（场地基频与输入卓越频率双控）标定，比例阻尼参数逐层写入材料。

**A5. 三段式验证协议（必须，论文可信度的根基）。**
1. **平坦层状场地** vs Thomson-Haskell 一维理论解：传递函数逐频对比，幅值误差 <5%；
2. **均质坡地退化情形** vs v3 已有结果：回归测试，曲线 MAE < 网格误差量级；
3. **文献复现**：选 2025 SDEE 层状坡地论文或 Brennan 线性分析中的 1~2 个算例定量复现。

### 3.2 参数化侧调整：对抗维度爆炸

全因子不可行：现有 3 维已是 175 工况；多层至少新增 3~4 维（厚度比、阻抗比、软层 Vs、层数），全因子直接 10⁴ 量级。对策：

1. **固定地层模板**，硕士阶段只做"两层：覆盖层 + 基岩"（这是规范场地分类的骨架，也是文献可对比性最强的构型）。三层以上写进展望。
2. **参数全部无量纲化**：d/h（覆盖层厚/坡高）、α=ρ₂Vs₂/ρ₁Vs₁（阻抗比）、η=h/λ、Tp/Ts。
3. **拉丁超立方（LHS）/Sobol 序列采样**替代规则网格，首批 120~180 个工况；之后用 C5 主动学习增补 50~100 个。总计 200~300 个工况 ≈ 个人电脑 3~4 个月夜间产能，可行。
4. 每个几何只跑**1 次宽频脉冲**（按 C1 提取传递函数），不再每个几何 × 3 条波，**数据效率直接提高 3 倍**。另留 10~15 个工况跑真实波，用于验证"H(f) 卷积合成 vs 直接 FEM"的闭环误差。

### 3.3 ML 侧调整

1. **特征向量扩展**：[η, i, angle, d/h, α, Tp/Ts, ...]，预计 8~10 维，仍在 GPR/XGB 舒适区。
2. **目标改为二维场 H(f, x/h)**：161 空间点 × ~100 频点的曲面。降维用**二维 POD（对矩阵做 SVD）或张量分解**，v3 的实测经验（161 维曲线 15 个主成分重构 MAE 0.012）表明这条路是通的，曲面预计 20~40 个模态足够，需实测验证。
3. **残差学习**（C3）：目标 = log[H_FEM ÷ (H_1D × H_topo_v3)]，取对数让乘性分解变加性、量纲归一。
4. **迁移衔接**：v3 的 GPR 核长度尺度、特征工程、GroupKFold 流程全部复用；均质工况可作为多层数据集的 α→1、d/h→任意 的退化子集**并入训练集**，白赚 174 个样本。
5. **TAF_v 根治**（顺手解决短板 S3）：放弃"竖向坡面 ÷ 竖向平地"的病态定义，改用 (a) V/H 比（坡面竖向 ÷ 坡面水平，分母远离零）或 (b) 竖向坡面 ÷ **输入波 PGA**（归一化基准恒定）。在频域框架下问题自动消失——竖向传递函数 H_v(f, x/h) 本身就是良态的。

### 3.4 数据与工程管理

工况命名沿用 `fuke-ALL-h{h}_i{i}_angle{angle}` 风格扩展为 `...{d}_{alpha}`；每批夜间任务生成 manifest（参数表 + 完成状态 + 校验和）；ODB 只提取所需时程后归档删除（个人电脑磁盘有限）；提取脚本一次写对：**地表全部节点的加速度时程**都存（频域框架需要全时程，不能只存 PGA——这是 v2/v3 数据管线最大的一处改动）。

---

## 第四部分 时域+频域特征纳入 ML 目标：可行性论证

结论先行：**频域目标不仅可行，而且应该成为主线**（即 C1）；时域特征目标可行、作为辅助；全时程直接预测不建议作为硕士主线。

### 4.1 频域目标（推荐为主目标）

| 候选目标 | 可行性 | 说明 |
|------|------|------|
| 传递函数 H(f, x/h) | ★★★★★ | 线弹性下与输入波无关——唯一能根治三波困境的目标；宽频脉冲一次提取 |
| 谱放大曲面 Sa比(T, x/h) | ★★★★☆ | 工程师更熟悉；但与输入波弱相关（阻尼单摆非线性映射），保留为 H(f) 的派生输出 |
| 卓越频率漂移 Δf_p(x/h) | ★★★★☆ | 标量曲线、好学；直接反映地形使场地频率特性改变，规范完全没覆盖，论述价值高 |

技术要点：H(f) 在低能量频段（输入谱接近零处）的估计会发散，需用宽频输入保证 0.5~10 Hz 全覆盖，并以相干函数筛除不可靠频点。

### 4.2 时域特征目标（推荐为辅助目标）

| 候选目标 | 可行性 | 说明 |
|------|------|------|
| Arias 强度放大曲线 I_a比(x/h) | ★★★★☆ | 与坡体永久位移（Newmark 类分析）直接挂钩，工程下游明确 |
| 持时变化 ΔD₅₋₉₅(x/h) | ★★★★☆ | 地形导致波被"困住"延长震动，是文献明确报道但少有预测模型的现象 |
| PGV、CAV 放大曲线 | ★★★☆☆ | 顺手可得，作为多目标输出一起回归 |

注意：时域特征是输入波相关的，所以它们仍受三波限制——**正确做法是由预测的 H(f) 卷积任意输入波后在时域计算这些特征**（虚拟传感器范式），而不是直接学"几何 → 时域特征"的映射。这样时域目标自动继承频域目标的波无关性。

### 4.3 全时程预测（不建议作主线）

直接学"输入 → 4000 步时程"就是 v2 已经failed的路线。但在 C1 框架下，全时程是**免费副产品**：预测 H(f) × 输入波 FFT → IFFT 即得地表任意点合成时程。论文里给 2~3 个工况展示"合成时程 vs FEM 时程"的对比图（含相位），是非常抓眼球的验证图，成本为零。

### 4.4 前置条件核查（立即可做）

频域路线唯一的硬前提：**拿到地表节点加速度全时程**。三种途径按成本排序：① 检查现有 ODB 是否还在（若在，写一个 odb 提取脚本即可，零计算成本）；② 若 ODB 已删，挑 10~20 个代表性几何重跑验证概念（1~2 周夜间）；③ 概念验证通过后再全量 174 个几何跑宽频脉冲。

---

## 第五部分 实施路线图（18 个月版，按月可调）

> 产能假设：单算例（均质、现网格）夜间可并行/串行完成 5~15 个 → 每月 100~300 个；多层细网格算例按 1/3 产能折算。每阶段都有"最低交付"兜底，进度落后时砍范围不砍质量。

### Phase 0（第 1 个月）：收割现有成果

- 把 v2 vs v3 对照整理成论文①初稿（C6，数据全部现成）。
- TAF_v 重定义（V/H 或除以输入 PGA），在现有数据上重算重训——预计 1 周，顺手补一个论文小节。
- 核查 ODB 留存情况（4.4 节途径①）。
- **交付**：论文①投稿稿 + TAF_v 修复版指标。

### Phase 1（第 2~4 个月）：频域框架概念验证 → 全量（C1+C2）

- 第 2 个月：宽频脉冲输入方案设计（Ricker 中心频率、时长、滤波）；在 10~20 个几何上做概念验证：脉冲提取的 H(f) × 三条真实波频谱 → 合成 PGA/TAF vs 既有 FEM 结果，闭环误差目标 <5%。
- 第 3 个月：174 个几何全量宽频脉冲计算（夜间批量，1 个月产能内）。
- 第 4 个月：二维 POD + GPR 训练 H(f, x/h) 代理；无量纲特征（η、Tp/Ts）替换绝对特征；重做留一高度测试验证 C2 收益。
- **成功标准**：任意波合成 TAF_h 的 MAE ≤ 0.03（达到当前 GPR 内插水平，但对**任意波**成立）；留一高度 h=10/400 的 R² 由负转正。
- **交付**：论文②的全部计算与图表；`train_v4.py`（按项目命名规范，大改动开新版本）。

### Phase 2（第 4~7 个月，与 Phase 1 尾部重叠）：多层 FEM 开发与验证

- 层状自由场求解器（传递矩阵 + IFFT）+ 均质退化对拍。
- VAB 脚本升级（逐节点局部材料参数）→ `VAB_layered_v1.py`。
- 三段式验证（3.1 节 A5）：一维理论 / v3 回归 / 文献复现。
- **成功标准**：平坦层状场地传递函数 vs 理论解误差 <5%；均质退化 vs v3 结果 MAE < 0.01。
- **交付**：验证报告章节（学位论文第 5 章第一节）。

### Phase 3（第 7~11 个月）：多层参数扫描 + 主动学习（C5）

- 两层模板，无量纲参数空间 [η, i, angle, d/h, α]，LHS 首批 150 个工况（约 2 个月夜间产能，细网格折算）。
- 首批训练 GPR → 主动学习闭环 2~3 轮，每轮 30~50 个工况。
- 对照实验：同预算下 主动学习 vs 随机采样 的精度曲线（C5 的论文证据）。
- **成功标准**：H(f, x/h) 曲面预测的频段平均 MAE 相对误差 <10%；主动学习相对随机采样有可量化优势。
- **交付**：多层数据集 + 采样方法对比结果。

### Phase 4（第 11~14 个月）：耦合残差学习 + 规范对比（C3+C4）

- 残差分解 R_coupling = H_FEM ÷ (H_1D × H_topo)，ML 学残差；与"直接学总放大"对照，验证数据效率优势。
- R_coupling 的参数敏感性分析：哪些 (α, d/h, η) 区域耦合最强？给出耦合显著区域图。
- 与 GB 50011 局部突出地形放大系数、Eurocode 8 ST×S 相乘假定对比，量化偏不安全/过保守区间。
- GPR 不确定性校准 + 适用域判别器。
- **交付**：学位论文第 6 章（重心章）+ 论文③初稿。

### Phase 5（第 14~18 个月）：收尾

- 工具化封装：单文件预测脚本（输入几何+地层+任意波 txt → 输出全场曲线+置信带+适用域警告），作为论文的工程落地展示。
- 学位论文撰写、答辩;论文③投稿。

### 里程碑依赖关系

```
Phase 0 ──► Phase 1（频域PoC不过关则回退：扩波库20条+现框架，论文②降级为波库扩展研究）
                │
                ▼
            Phase 2 ──► Phase 3 ──► Phase 4 ──► Phase 5
            （Phase 2 验证不过关则多层范围缩为"两层+垂直入射"）
```

---

## 第六部分 衍生问题与展望

**Q1 土体非线性怎么办？** 线弹性是 C1 波无关性的前提。诚实处理：(a) 论文明确适用域为中小震/岩质及硬土坡（线性假设成立区）；(b) 展望章给路线——等效线性化修正系数，或以线弹性 H(f) 为基底、ML 学非线性偏移量（残差思想的再次复用）。不要在硕士阶段开伪非线性战场。

**Q2 3D 效应？** 二维平面应变对垂直坡走向入射是合理近似，斜交入射和三维地形（山脊、凸角）放大更强。作为展望，并在论文适用域声明中写明。

**Q3 怎么回应"你只是又一篇 ML 调参论文"的质疑？** 答辩武器按序：① 物理重定义目标（C1）而非换模型；② 无量纲化带来的外推能力提升有 before/after 实测对比（C2）；③ 残差学习的科学产出是规范耦合误差的量化（C3）；④ 全流程基线齐全（B1/B2/B3）+ GroupKFold + 留一外推 + UQ 校准，方法学上无懈可击。

**Q4 可重复性？** 延续现有好习惯：所有结论锚定 JSON 实测文件、固定随机种子、manifest 管理工况、诊断脚本只读可复跑。建议论文投稿时附数据与代码仓库（去除原始 ODB，保留特征级数据）。

**Q5 论文①②③万一只完成一部分？** 学位论文的最低安全配置 = 论文①（已齐）+ Phase 1 频域框架（均质坡地版）。多层部分即使只完成 Phase 2 验证 + 小规模扫描，也足以支撑"初步研究 + 展望"章节。路线图的每个 Phase 都设计为独立可写章节，不存在"全有或全无"风险。

---

## 第七部分 风险登记表

| # | 风险 | 概率 | 影响 | 缓解 |
|---|------|------|------|------|
| R1 | ODB 已删，频域路线需重跑 | 中 | 拖延 2~4 周 | 途径②小批量重跑即可，全量也仅 1 个月产能 |
| R2 | 宽频脉冲提取的 H(f) 与真实波反演不一致（数值阻尼/边界污染） | 中 | C1 根基动摇 | Phase 1 概念验证先行，闭环误差 >5% 则诊断（Rayleigh 阻尼标定、加密网格、延长静默段） |
| R3 | 层状斜入射自由场推导出错 | 中 | Phase 2 阻塞 | 均质退化对拍 + 一维理论对拍双保险；入射角限制在临界角内 |
| R4 | 多层细网格单算例过慢，产能腰斩 | 中 | Phase 3 规模缩水 | 截止频率从 10 Hz 降到 8 Hz；软层 Vs 下限抬高；主动学习本就为小预算设计 |
| R5 | 二维 POD 对 H 曲面压缩效果差 | 低 | 改逐频独立建模 | 先实测奇异值衰减曲线再定模态数；备选：对 log\|H\| 压缩 |
| R6 | 硕士时间不足 | 中 | 范围裁剪 | 按 Q5 的最低安全配置裁剪，先保①+Phase 1 |

---

## 附录 与现有文件的衔接

- 本报告承接 `ml_plan_v3.md`（其 Stage 0~4 已全部 ✅），是其后续战略层文件。
- 代码版本规划：频域框架 → `train_v4.py`（大改动开新版）；多层建模 → `Modeling/Layered/VAB_layered_v1.py`（新目录）；v2/v3 文件与 `outputs_v2/`、`outputs_v3/` 全部保留不动。
- 本报告引用的全部 v3 数字可由 `diagnose_v3.py`、`baseline_v3.py`、`train_v3.py`、`evaluate_v3.py` 复跑核验。

## 参考文献线索（撰写论文时补全卷期页码）

1. Prediction framework of slope topographic amplification on seismic acceleration based on machine learning algorithms. *Engineering Applications of Artificial Intelligence*, 2024. （证明"ML 预测坡地放大"已有先例，须差异化）
2. The combined amplification effects of topography and stratigraphy of layered rock slopes under vertically and obliquely incident seismic waves. *Soil Dynamics and Earthquake Engineering*, 2025. （耦合方向最新参数分析，你的 ML 化是其自然延伸）
3. Coupling of topographic and stratigraphic effects on seismic response of slopes through 2D linear and equivalent linear analyses. *Soil Dynamics and Earthquake Engineering*, 2014.
4. Generalized deep neural network for seismic site response prediction with transfer learning. *Engineering Applications of Artificial Intelligence*, 2025.
5. Machine learning based ground motion site amplification prediction. *Frontiers in Earth Science*, 2023.
6. Topographic amplification of ground motions incorporating uncertainty in subsurface soils. *Soil Dynamics and Earthquake Engineering*, 2021.
