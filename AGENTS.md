# AGENTS.md

## 基本要求

- 始终使用简体中文回复。
- 本文件规则与用户本次明确要求冲突时，以用户要求为准。

## 文档

- 用户提及 `docs/` 中的内容时，执行前先阅读相关文档；默认只读 Markdown，明确提及的参考文献或其他格式除外。
- 修改项目功能、逻辑、参数或代码后，同步更新 `docs/` 中的相关文档；计划变更或完成后同步更新状态。
- 文档统一放入 `docs/<类型>/`：`交接文档`、`技术文档`、`计划文档`、`论文材料`、`进度汇报`、`参考文献`。生成文档的脚本与其输出放在同一子目录。
- 技术报告、指南、计划等采用描述性中文文件名，不加 `_v1`、`_v2` 等版本后缀，直接更新现有最新版。
- `进度汇报/` 文件名以 `YYYY-MM-DD_` 开头。
- Word 是最终阅读成果，不得删除含关键研究数据的 Word 文件。`论文材料/` 优先保留 `.docx`；Markdown 转为 Word 后，确认正文和图片已完整内嵌，必须删除过渡 Markdown 与独立图片目录。

## 目录

- 建模脚本：`Modeling/<模块>/`。
- 后处理脚本：`Postprocess/<模块>/`，现有模块为 `General`、`Multi`、`Single`、`Hybrid`。
- 批处理脚本：`Batch/Autorun_*.py`。
- 测试：按下节规则放入 `test/`。
- 移动依赖 `__file__` 定位主脚本或输入波的测试脚本时，必须同步修正相对层级；移动后运行 `py_compile`，并模拟新 `__file__` 验证目标路径存在。

## 测试

- 不调用 Abaqus/OpenSees 求解器的纯 Python 测试放入 `test/<模块>/`，可正常入库。
- OpenSees 脚本及产物统一放入 `test/OpenSees/`；Abaqus 脚本及产物统一放入 `test/Abaqus/`。两目录整体由 `.gitignore` 排除。
- Abaqus 测试入口放入 `test/Batch/`，以 `Batch/Autorun_template_v2.py` 为模板，可由普通 Python 直接启动：`sys.argv[1]` 接收工况根目录，缺省时使用脚本所在目录；脚本路径和工况路径均使用绝对路径；产物写入 `test/Abaqus/<工况>/`。
- 运行形式：`python <仓库绝对路径>\test\Batch\Autorun_xxx_v1.py <仓库绝对路径>\test\Abaqus\xxx-test`。

## 代码

- 新增或修改的注释统一使用中文；只注释关键逻辑与配置参数，措辞简洁准确，不注释显然代码、空行、括号行或语法分隔行。
- 优先使用行内注释；函数参数、返回值和用途集中写入 docstring。配置字典项只保留一行行内注释，取值含义用 `/` 分隔，详细推导写入 docstring 或文档。
- 代码文件使用简洁的英文下划线命名。脚本以 `_v{版本号}` 结尾；小改直接更新当前版本，大型功能或架构变更新建版本并保留旧版。
- 默认保持脚本自包含、单文件。除非用户明确要求，不拆分模块；少量通用辅助函数直接内联，优先减少文件和依赖。
