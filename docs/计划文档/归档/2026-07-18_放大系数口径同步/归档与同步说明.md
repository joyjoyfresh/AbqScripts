# 放大系数口径同步与归档说明

## 归档原因

`成层场地地形放大系数偏差诊断与优化方案.md` 面向旧版 `VAB_oblique_TAF_multilayer_v6/v7.py` 编写，其中关于水平/随地形成层、一维自由场基准和总放大—纯地形放大区分的有效内容，已落实到当前建模、后处理和论文第三章。旧文件中的脚本路径、待开发清单和图15目测结论不再适合作为现行实施计划，因此转为历史诊断材料保存。

## 本次同步内容

- 第三章明确区分基岩露头运动归一化的总放大系数 `AF_h`、同侧一维场地放大基准 `AF_h,1D` 与纯地形放大系数 `TAF_h`。
- 第三章后续数值解验证统一规定 `R(s) ≡ TAF_h(s)`，原V3数值、误差指标和验收阈值保持不变。
- 建模脚本的 `ff_normalization` 元数据新增 `af_h_definition` 与 `taf_h_definition`，并修正旧的TAF命名说明。
- `ff_theory.left/right.taf_h/taf_v` 继续作为兼容字段保留，其物理含义明确为相对于基岩露头运动的同侧一维 `AF_1D` 基准。

## 文件说明

- `成层场地地形放大系数偏差诊断与优化方案.md`：原历史诊断方案。
- `同步前_第3章_斜入射成层坡地有限元模型与可信性验证.md`：第三章修改前快照。
- `同步前_slope_frame_ssi_full_v2.py`：建模脚本修改前快照。

## 验证结果

- 建模脚本通过 Python 语法编译检查。
- `test/Hybrid/test_postprocess_all_surface_v2.py` 的12项测试全部通过。
- 活动文件中未再发现 `TAF_h=PGA_h/(factor_h*PGA_in)`、`TAF=PGA/(factor_h×PGA_in)` 或第三章旧 `R(s)=PGA_h/PGA_in` 表述。
- V3验证程序仍读取原 `TAF_h` 字段，既有结果无需重新计算。
- `git diff --check` 通过。
