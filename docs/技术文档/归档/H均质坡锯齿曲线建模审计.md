# H均质坡锯齿曲线建模审计

_审计日期：2026-07-26—2026-07-28；对象：`Modeling/slope_frame_ssi_full_v2.py`、H1—H3 `run-001/run-002/run-005`结果与H正式入口。_

## 结论

本轮先检查建模脚本，不以新增一天级Abaqus试算代替调试。生产P–SV单柱求解器、边界积分、外法向应力和`Ku+Cv+Aσ`分量映射均未发现代数实现错误，但进一步审查识别出一个比“底边三种柱高怎么选”更根本的模型形式错误：旧`local_columns`把左侧上平台柱、右侧下平台柱和底边连续变高柱拼成所谓自由场，它们不是同一个二维波动方程解。等效节点力公式要求位移、速度和应力来自同一个背景自由场，仅做到角点数值连续并不够。

主脚本现新增`reference_field_mode`，H正式入口冻结为`global_upper`：左、右、底三条人工边界全部从同一个以上平台为自由面的平场解取值，实际坡面和下平台相对该背景场产生的反射、转换和散射只由有限元域计算一次。旧`local_columns`只保留用于历史复现，不再用于H正式数据。既有H `run-003`复制的是修复前脚本，仍不得启动；这次修复先由纯Python平面波闭合测试验证，随后在`run-005`完成3个Abaqus代表工况预生产质量门并正式放行。

## 已排查项目

| 项目 | 方法 | 结果 |
| --- | --- | --- |
| 频域P–SV全局矩阵 | 与不导入生产脚本的独立参考程序逐频对拍 | 18组最大复数相对误差`1.538e-15` |
| 瑞利阻尼复参数 | 同时覆盖弹性、质量比例和刚度比例项 | 与独立参考一致 |
| 自由面与界面条件 | 检查位移/应力连续及自由面零牵引方程 | 未发现符号或排列错误 |
| 边界外法向 | 左`(-σxx,-σxy)`、右`(+σxx,+σxy)`、底`(-σxy,-σyy)` | 与二维应力张量乘外法向一致 |
| 等效节点力 | 检查侧边和底边的法/切向`Ku+Cv+Aσ`映射 | 纯Python回归通过 |
| 边界积分权重 | 线性tributary与二次边`1/6:2/3:1/6` | 总影响长度守恒 |
| 空间统一网格 | 将规范`s`网格与原始节点线性插值直接比较 | 误差为机器精度量级，未制造额外锯齿 |
| 网格与能量 | 4 m网格、基岩`Vs=2000 m/s`、10 Hz最短S波波长200 m | 约50单元/最短波长；`ALLAE`占比很小 |
| 扩域输入相位 | 比较`side_clearance=0.1/1/4h`时坡肩、坡脚相对`phase_origin_x=center`的位置 | 完全不变，扩域对比没有因相位原点平移而偷换坡体输入 |

## 阻尼配置问题与修复

H旧工况配置写有`constant_xi=0.03`，但主脚本一直把该参数限定为“有限土层统一阻尼”；H工况为`layers=[]`，所以实际均质材料没有采用3%阻尼。`run-001/run-002`的`case_meta.json`记录的实际值均为`Q=999、xi=0.0005005005`。这不是求解时随机失效，而是配置表达容易让人误读。

H均质坡承担“去除覆盖层后的基岩坡`H_topo`”角色，需与成层模型中的基岩材料保持一致，本轮不把它事后改成3%并制造新的基线。修复内容为：

- H正式入口显式写为`constant_xi=None、q_bedrock=999、bedrock_xi=None`，物理结果与旧H一致；
- 主脚本新增可选`bedrock_xi`，确需固定基岩阻尼时可显式设置；
- 日志和`case_meta.json`记录配置作用域及每层实际`Q/xi/alpha/beta`；
- 非法阻尼比和品质因子直接中止，不再静默进入材料定义。

因此，阻尼语义已经修复，但这项修复本身不会把旧H曲线变平滑。

## 为什么宽频曲线比旧射线法曲线起伏更多

对同一模型、几何和自由场引擎的既有数据计算二阶空间粗糙度，2 Hz、4 Hz、8 Hz Ricker分别约为`0.0018、0.0035、0.0079`，宽频多正弦为`0.0141`。频带越宽，多个频率成分在坡肩、坡面和坡脚形成的相干散射峰谷越密，PGA又取各空间点完整时程的绝对峰值，峰值发生时刻可在相邻位置切换，因此空间PGA曲线天然比单一Ricker结果更起伏。

把当前H结果仅用于诊断地限制到既有Ricker共同可靠上限约4.8 Hz后，水平曲线粗糙度由H1/H2/H3的`0.0209/0.0145/0.0136`降至`0.0038/0.0039/0.0035`。这说明大部分“锯齿感”来自宽频高频分量和峰值取法，而不是空间插值。该低通结果只能用于解释来源，不能替代原始0.5—10 Hz结果或被当成正式修图。

## 底边参考场根因与修复

当前`bottom_ymax_mode=local`在每个底边节点使用其正上方真实地表高度构造一维自由场柱。这样能保证左下角与上平台自由场一致、右下角与下平台自由场一致，并在坡面投影范围内连续过渡；但它也使底边输入随坡面高度变化，相当于参考场已经含有一部分与坡形相关的相位和幅值变化。

以H1代表参数`h=200 m、i=60°、θ=30°、side_clearance=1h`进行无Abaqus解析扫描：

| 底边柱高口径 | 水平底边自由场PGA范围 | 相对跨度 |
| --- | ---: | ---: |
| `local` | 1.767343—1.835842 | 3.762% |
| `upper` | 恒为1.835842 | 0 |
| `lower` | 恒为1.812046 | 0 |

上述时域PGA跨度只有`3.762%`，曾使问题看起来像“可接受的局部近似”。但PGA把不同频率和不同时刻的峰值折叠在一起，会严重掩盖频率逐点的不一致。对同一`H_lower=600 m、H_upper=800 m、Vs=2000 m/s`均质坡做垂直入射单频检查，旧局部柱在底边同一高程的水平自由场幅值跨度为：

| 频率 | `local_columns`底边幅值跨度/最大值 | `global_upper` |
| ---: | ---: | ---: |
| 0.5 Hz | 47.427% | 0 |
| 1.0 Hz | 61.803% | 0 |
| 2.0 Hz | 100.000% | 0 |
| 4.0 Hz | 100.000% | 0 |
| 8.0 Hz | 100.000% | 0 |
| 10.0 Hz | 100.000% | 0 |

垂直入射时，任一固定高程上的平面自由场应与`x`无关。旧口径却令柱顶高度成为`H(x)`，使底边场写成`F(y;H(x),f)`；在坡面投影区，其`x`导数额外包含`H'(x)∂F/∂H`，而脚本计算的应力仍来自各自一维柱，没有包含这部分空间导数。因此，旧位移场与旧应力场并非同一二维解，不能共同代入等效节点力公式。高频处恰好会在不同柱高上跨越多个驻波波节，足以直接制造密集的频率—空间起伏，这与既有宽频H曲线的锯齿增强现象一致。

修复后的`global_upper`具有三个明确性质：

1. 三条人工边界统一使用`F(y,f)exp(-iωpx)`，位移、速度与应力来自同一个背景解；
2. 背景场只包含上平台平场传播，不预含真实坡形，坡面散射不会在边界输入和有限元自由表面中重复计算；
3. `global_lower`不能用于当前单面坡，因为左边界存在高于下平台自由面的节点，脚本现会直接拒绝这种无物理意义的外推。

这不是为了让曲线“看起来更平”而选参数，而是按等效节点力波场分解的数学前提修复参考场。公开的抬升地形斜入射实现同样以单一参考自由面生成左、底、右三条边界的入射/反射波场，而不是逐`x`更换自由面；其自由场与散射场叠加为总场的推导可见[Cao等（2022）](https://doi.org/10.3390/sym14112244)。同时应保留适用范围：局部VAB自由场输入仍假定散射波能被人工边界充分吸收，高频和大角度地形散射可能需要总运动场输入或边界积分耦合，相关局限见[Wang等（2024）](https://doi.org/10.1016/j.soildyn.2024.108901)。所以`global_upper`关闭的是“拼接场不自洽”问题，不等于取消后续计算域收敛门。

另一个可能的干扰已经排除：H入口采用`phase_origin_x=center`，而左右侧净空是对称增加的。坡肩相对相位原点的位置恒为`0.5h-w_slope/2`，坡脚相对位置恒为`0.5h+w_slope/2`，均与`side_clearance`无关；纯Python回归对`0.1/1/4h`三档结果完全一致。因此`run-001/run-002`的差异不能归因于扩大域时偷偷改变了坡体处输入相位。

## run-005首批QA复核与正式放行

`run-005`的H1、H2、H3代表工况均已完成求解和当前规范数据提取，3个工况的`frf`与`energy`全部通过。此前失败项来自首批配置中的`domain`：该规则用不同坡高、不同坐标体系的工况互相比较，而且H1、H3又相对旧`run-002 local_columns`报告`26.680%/25.649%`的`TAF_h_comp`差，违反计算域收敛试验的单变量原则。这些差异只表示模型修正量，不能解释为`4H`计算域不收敛。旧规则现已作废，3个既有ODB仅用当前后处理重分类，没有重新调用Abaqus；备份和散列迁移写入`H1_H3旧域门重分类审计.json`。

H1、H3另有传播上游端点PGA相对一维自由场约`29.2%`和`48.4%`的诊断差异。斜坡模型端点保存的是入射场与坡体散射场之和；二维坡体会向上游产生反射波和表面波，因此坡地端点总响应不具备强制等于无坡一维场的理论条件。该端点量只保留为散射诊断，不作为H坡地正式放行门；`run_cfg.validation_geometry='flat'`的平场对拍仍继续执行一维闭环。

`geometry_cfg.side_clearance=c`表示在坡顶`4H`和坡脚`3H`观测窗以外，左右两侧各追加`cH`净空。以H3坡角`75°`为例，`c=1/2/4/6`时模型总宽分别约为`9.268H/11.268H/15.268H/19.268H`；`c=1`时人工边界实际已距坡肩`5H`、距坡脚`4H`。因此旧`0.1H→1H`结果有差异只能证明`0.1H`不足，不能证明`1H`仍不足；继续把已很大的`4H`扩到`6H`不能回答经济生产域能否缩小，且会显著浪费网格与求解时间。

计算域收敛据此采用事前冻结的最不利代表工况：复用`run-005/H3-000006`的`global_upper + 4H`大域结果作为参考，候选域保持坡高`200 m`、坡角`75°`、入射角`30°`不变，唯一改变量为`geometry_cfg.side_clearance`。第一阶段检验`1H`；其未通过后，基于成本—效应量复核再检验最后一个`2H`候选。比较范围始终为统一`s∈[-4,4]`研究窗口和`0.5—10 Hz`共同有效频带，强制同时检查`log|H|`、相位圆周误差和`TAF_h_comp`，不再只比较PGA单一标量。两阶段采用同一组预注册门槛：

| 指标 | 门槛 |
| --- | ---: |
| 共同有效复频响覆盖率 | `>=95%` |
| `log|H|` RMSE | `<=0.05` |
| 相位圆周RMSE | `<=0.20 rad` |
| `|Δlog|H||` P95 | `<=0.10` |
| `|Δφ|` P95 | `<=0.35 rad` |
| `TAF_h_comp`相对差P95/最大值 | `<=5%/10%` |

专用入口为`Run/Auto_ch4/Autorun_ch4_H_domain_v1.py`，正式目录为`Run/ch4_H_domain_convergence/run-001`。原`6H`目录从未启动Abaqus且没有过程产物，已在求解前改为`1H`候选域并重新冻结参考NPZ、配置、模型、后处理、输入和判据散列。该工况已完成，FRF、能量、规范NPZ/Excel及压缩包完整性均通过；过程文件清理释放约`2.09 GiB`并保留CAE。

1H与4H共同有效覆盖率为`100%`，相位圆周RMSE=`0.12219 rad`、相位差P95=`0.16516 rad`，`TAF_h_comp`相对差P95/最大值=`3.796%/5.353%`，均通过；但`log|H|` RMSE=`0.09959>0.05`，幅值对数差P95=`0.16422>0.10`。依照求解前冻结的全项联合门，1H判定为不通过，不能用于正式生产。

4H H3模型为`135264`个单元，1H模型为`82764`个单元；按相同网格规则，2H约为`100264`个单元，比4H少约`25.9%`。2H最不利工况已按预注册规则完成：覆盖率为100%，相位RMSE/P95=`0.09180/0.12810 rad`，TAF差P95/最大=`3.168%/5.129%`，均通过；但`log|H|` RMSE=`0.08822>0.05`、幅值对数差P95=`0.13004>0.10`。因此按当时“完整表面原始FRF全项通过”的单一口径，2H仍不能替代4H并冻结为4H；后续限定输出决策不改写该历史判断。

3个代表工况的FRF均为767/767点通过，能量平衡残差最大`1.38e-6`、人工能占比最大`1.46e-6`。`H1_H3预生产放行门.json`已冻结1H/2H域门、4H生产域、3个配置和规范结果散列。删除前另行记录ODB、STA和CAE散列，随后永久删除30个过程文件，释放`11.638 GiB`；CAE、NPZ、Excel、质量JSON和清理审计均保留。`run-005`当前为3个`pipeline_passed`、48个`planned`。

2026-07-28用户选择把2H作为限定输出生产候选。该决策不改写上段完整表面原始FRF域门的失败结论，而是把用途分为`s≤1`原始FRF、全表面固定频带RMS、水平PGA/TAF、可选近坡面带宽平均FRF和只作诊断的完整原始FRF。新`run-006`已生成51份2H配置，当前51/51为`planned`且没有求解产物；首批只允许运行H1/H2两个独立配对。

## 后续执行约束

1. 冻结H `run-003`，不运行其中3个代表工况；目录内仍是审计前脚本，51个状态保持`planned`。
2. 主脚本和H入口已改为`reference_field_mode=global_upper`；纯Python回归要求垂直入射时三边界使用同一柱解、底边定高程幅值不随`x`漂移。
3. 主脚本默认值进一步加固为`global_upper`后，先生成的`run-004`因来源散列随之过期且未启动；最终预生产目录为`run-005`。其中51份配置均为`reference_field_mode=global_upper`，复制主脚本散列与当前源文件一致。
4. `run-005`中H1/H2/H3代表工况均已通过FRF和能量QA；旧`run-002 local_columns → run-005 global_upper`对比不得再命名为计算域收敛。
5. H3最不利`1H`和`2H`均未通过完整表面原始FRF幅值门；该失败标签保持不变，不把2H限定输出选择改写为完整域收敛。
6. `run-005`剩余48个4H工况暂停。`run-006`首批只能由`--run-pilot`领取H1/H2两个配对；两组输出级门全部通过后，`--run-remaining`才可领取其余49个2H工况。
7. 正式论文图同时保留原始PGA/TAF、频率分辨的`FSAF=|H|`及代表频率切片。带宽平均FRF必须单独命名、记录`σ=0.10 Hz、s≤2.8`，不得用平滑曲线替换规范原始数据。
