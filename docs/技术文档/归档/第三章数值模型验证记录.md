# 第三章数值模型验证记录

本文档记录第三章有限元平台的逐单元执行证据。它与论文正文分离：只有同时通过相应物理验收门槛的结果才可进入论文；功能闭环通过不等同于方法有效性通过。

## 运行目录现状

2026年7月18日按第三章最终正文的图表与数值来源清理 `Run`。当前以 `ch3_` 开头的结果目录仅保留直接进入正文证据链的 `ch3_V1_geometry_material`、`ch3_F1_frequency_theory`、`ch3_F0_06_energy`、`ch3_V3_slope_solution_verification` 和 `ch3_V4_external_slope_benchmark`；`Auto_ch3` 保留正式入口与论文处理脚本。各正式结果目录内部也只保留被第三章图表读取的运行批次。

F1正式验算的2 Hz和4 Hz输入已冻结在 `Wave/Impulse/Acceleration/F1_frequency_validation`。文件名分别为 `ricker_2Hz_t0_2T_duration4s_dt1ms.txt` 和 `ricker_4Hz_t0_2T_duration4s_dt1ms.txt`，SHA-256分别为 `27c94e1ac73c2dc9cdb407951de451674eb7c7f7aa5b7a9d8bb908a633cd44a4` 和 `1f3e4dc424f960e64c7db0187cc2b3a8878046e51d9e8da7caaf1e618d8d5115`。入口复制到算例目录时仍使用 `ricker_wavelet_2Hz.txt` 和 `ricker_wavelet_4Hz.txt`，以保持既有记录名和后处理字段兼容；原 `Run/_f1_generated_waves` 缓存目录已移除。

| 正式目录 | 正文实际采用批次 | 对应证据 |
|---|---|---|
| `ch3_V1_geometry_material` | `run-001` | 12组几何—材料组合审计 |
| `ch3_F1_frequency_theory` | `run-003/004/005/007/009` | A1—A5平场频域理论验算 |
| `ch3_F0_06_energy` | `run-001` | E1人工能量与能量平衡 |
| `ch3_V3_slope_solution_verification` | `run-002` | 11组坡地数值解验证 |
| `ch3_V4_external_slope_benchmark` | `run-002` | Shen等（2024）外部坡地基准 |

未进入最终正文的U0—U3前期流程、F0-1/F0-2基础回归、V2失败诊断及正式目录中的无效重跑已迁至 `test/Abaqus`；对应入口统一放在 `test/Batch`，默认输出也已改为 `test/Abaqus`。下文保留原始执行时间线，其中出现的旧 `Run/...` 路径表示清理前位置，现行位置以本节为准。Word/PDF分页渲染缓存和临时日志不属于数值证据，也已移入 `test/Abaqus`，不再占用正式运行目录。

## U0：Hybrid v2 全流程最小闭环

### 目的与工况

U0 用于验证参数注入、Abaqus 建模求解、ODB 后处理、最终数值包收集和统一绘图能否在独立目录中连续完成。工况采用 `freefield` 场景、均质基岩、坡高 50 m、坡角 45°、8 m 的 CPE4R 均匀网格、0° 入射和 4 Hz Ricker 输入波。该工况为了快速暴露接口错误而采用较小的观测窗外净空，因此不承担边界反射或物理放大规律的验证任务。

本次唯一基线为 `Modeling/slope_frame_ssi_full_v2.py`、`Postprocess/Postprocess_All_surface_v2.py`、`Postprocess/Collect_All_results_v2.py` 和 `Postprocess/Plot_Hybrid_surface_v2.py`。控制脚本为 `Batch/Autorun_ch3_00_smoke_v1.py`，实际通过工况目录为 `Run/ch3_00_smoke/run-005/`。

### 功能验收结果

建模脚本确认读取了 `case_config.json` 的材料、几何、阻尼、网格、时间步、自由场、运行控制和 `tssi_cfg` 八个配置段。FD 自由场引擎的均质半空间与单层解析对拍误差分别为 \(1.11\times10^{-16}\) 和 \(3.55\times10^{-16}\)。模型包含 315 个 CPE4R 单元、360 个节点和 36 个地表节点，输入增量为 0.001 s；按 10 Hz 的有效频率上限计，达到 100 步/周期。Abaqus 作业完成后，单工况后处理生成包含 8 张数值表的 `surface_results.npz` 和 Excel 工作簿，最终收集器写入 1 条 `SURFACE_RESULTS_NPZ` 索引记录，绘图脚本生成 32 张 PNG 图件。U0 因而通过“功能闭环”门槛。

### 发现的问题、修复与回归

首次静态接口审查发现，单工况后处理会将临时 CSV 打包为 `surface_results.npz` 后删除 CSV，而原收集器只搜索 CSV，导致统一绘图脚本无法获得其所需的 NPZ 索引。已在 `Collect_All_results_v2.py` 中增加对最终 NPZ 的搜集，并按包内 `manifest_json` 的 `sgrid_response_<record>.csv` 展开记录。随后发现质量摘要虽被打包在 `surface_summary_json` 中，但未被收集器读取，造成 `AR_max` 与 `suspect` 在 `index.csv` 中丢失。收集器现已从该字段恢复两项指标，并统一处理带或不带场景后缀的记录名。最小 NPZ 回归测试和对 `run-005` 的后处理回归均通过；最终索引记录为 `AR_max=1.12580110868248`、`suspect=True`。

系统 Python 调用收集和绘图脚本时还出现了中文日志编码不一致。控制脚本已为这两步显式设置 `PYTHONIOENCODING=utf-8`，回归后的 `autorun_03_collect.log` 和 `autorun_04_plot.log` 可按 UTF-8 直接读取。此前 `run-001` 至 `run-004` 的中止目录保留为执行托管问题的原始证据；它们未被视为模型失败或有效性结果。

### 质量判定与论文写入状态

虽然 U0 的数据链路通过，但该小域工况的远场 QA 未通过后续 U4 所需的 5% 门槛：左、右端相对误差分别为 9.71% 和 9.11%，后处理正确标记 `suspect=True`。结合 1 s 静默尾段和缩小的侧向净空，这一结果说明边界反射已进入峰值统计窗口；它验证了质量标记能发挥作用，而非证明边界条件有效。因此 U0 不产生论文正文中的验证图、表或结论，也不允许用其 1.1258 的放大值讨论坡地效应。

U0 的论文写入状态为“无正文写入”。下一步 U1 将在独立工况中验证无量纲几何换算、材料带、地表节点集、坡顶参考点与三段归一化观测坐标的一致性；只有 U1 的检查通过后，才开始撰写第 3.2—3.3 节。

## U1：几何、分层与观测体系实现

### 目的与工况

U1 用于核验“配置中的几何、材料和观测定义是否被有限元模型准确实现”，不以响应幅值规律作为判断对象。工况采用 `freefield` 场景、坡高 25 m、坡角 30°、坡顶和坡脚观测窗各为 2h、侧向净空为 1h、坡脚以下深度为 2h；介质采用 8 m 的表层和 12 m 的下伏层，且界面沿地形等厚铺设。网格采用 CPE4R 单元和分层渐变划分，输入为 4 Hz Ricker 波的垂直入射。

### 验收结果

无量纲换算得到坡顶平台长度 75.0 m、坡面水平投影 43.301 m、坡脚平台长度 75.0 m 和总长 193.301 m；`case_meta.json` 中的坡肩、坡脚位置与上述值一致。模型成功形成 3 个材料带和 2 条沿地形界面，网格统计为 4 009 个单元、4 114 个节点，`TOP_SURFACE` 节点集为 188 个节点。三段归一化观测坐标覆盖 \(-2\le s\le3\)，以 0.01 间隔输出 201、99、201 个点，共 501 个固定长度观测点。

`CREST_REF` 的坐标口径在本次验证中得到澄清：它不是坡肩节点，而是与默认三跨框架基础中心对应的 freefield 参考点。默认框架宽度为 18 m、偏移为零时，目标位置为坡肩向上平台内退 9 m；本工况目标坐标为 66.0 m，实际离散节点为 66.4 m，偏差小于一个局部网格尺寸。该定义保证后续 freefield、fixed 和 ssi 场景之间能够传递同一位置的地震动。

### 问题、修复与论文写入状态

U1 首次完成后，验收脚本因正则表达式的双重转义未能读取日志中的 `TOP_SURFACE=188`，并错误地把坡肩 75.0 m 当作坡顶参考点的目标。检查真实建模日志后，已修复正则并按主建模脚本的框架中心公式校验 `CREST_REF`；随后仅对既有 ODB 重跑后处理、收集、绘图和验收，U1 通过。该工况的最终质量标记为 `suspect=False`，但这只是当前观测窗内的内部质量信息，不构成 U4 的边界有效性结论。

U1 支撑的第3.2—3.3节已写入 `边坡地震动放大效应研究论文初稿（整合修订）.docx`，内容包括研究对象、基本假定、无量纲几何、沿地形分层、TOP_SURFACE、CREST_REF 和三段坐标协议。DOCX 内容结构检查通过，并已调用本机 Microsoft Word 导出 61 页 A4 PDF 完成分页复核。检查发现表3.1末行原先被跨页拆分，造成第28页出现空白续表行和孤立文字；修订稿现已禁止该表各行跨页拆分并启用重复表头，复核后第27—28页续表完整、正文接续正常。

## U2：斜入射输入与人工边界一致性

### 工况与通过项

U2 采用 15° 斜入射、均质基岩、坡高 25 m、坡角 30°的 `freefield` 工况。为排除 U0 小计算域的影响，坡顶和坡脚观测窗均取 2h，侧向净空取 2h，坡脚以下深度取 3h；模型总长为 243.301 m、共 352 个单元。建模元数据确认入射角为 15°；FD 自由场自检的半空间和单层误差仍分别为 \(1.11\times10^{-16}\) 与 \(3.55\times10^{-16}\)。日志确认左、右、底三侧均完成弹簧—阻尼器和等效节点力创建，且斜入射水平慢度为 \(1.294095\times10^{-4}\)。

### 未通过项与诊断结论

单工况后处理和 NPZ 收集均成功，但质量摘要给出左、右远场水平误差分别为 8.46% 和 7.42%，超过预设 5% 门槛，最终标记为 `suspect=True`。该结果与 U0 中的短域问题不同：U2 已显著加大侧向净空和底部深度，误差仍然存在。因此不能简单归因为计算域过小，也不能通过放宽阈值将其写成“边界验证通过”。

对 `Run/ch3_02_input_boundary/run-001/` 中全部33个原始地表节点继续检查后发现，误差并非集中在人工边界端点：左平台 TAF_h 从边界处 1.0846 连续增至坡缘附近 1.1584，右平台从坡缘附近 0.9805 连续降至边界处 0.9258。左、右端点误差甚至小于各自平台的最大误差，因此不能通过把 QA 点从端点移到相邻节点来“修复”判定。该空间梯度表明斜入射坡体散射与人工边界基准尚未被解耦。

为区分坡体散射传播距离与等效节点力实现问题，`Run/ch3_02_input_boundary/run-002/` 保持材料、入射角、网格和输入波不变，仅将侧向净空由 2h 增至 6h、坡脚以下深度由 3h 增至 5h。求解与全流程后处理均完成，左端误差由 8.46% 显著降至 0.79%，但右端误差仅由 7.42% 降至 7.07%，仍未通过。右平台 TAF_h 在远离坡脚后稳定在约 0.919—0.929，并未随距离继续趋近1。这一非对称结果否定了“统一扩大域即可解决”的假设，并提示误差与斜入射传播方向、坡体散射或固定侧边界实现有关。

`Run/ch3_02_input_boundary/run-003/` 因而采用单因素反向试验：保持 run-002 的 6h 侧向净空、5h 底部深度、材料、网格和输入波不变，只将入射角由 +15° 改为 −15°。结果左端误差为5.40%，右端误差为1.82%，误差较大侧随传播下游方向由右侧交换至左侧，排除了固定右边界实现错误。

据此，斜入射边界 QA 改为双向配对口径：垂直入射仍要求两端同时与一维自由场对拍；斜入射只以传播上游端检验输入和人工边界一致性，下游端保留为坡体散射响应并继续报告，但不再错误地要求其峰值回到一维自由场。按预先规定的2%门槛，+15° 工况的左上游误差为0.79%，−15° 工况的右上游误差为1.82%，两侧边界分别作为上游端时均通过；FD 半空间和单层自检误差仍为 (1.11\times10^{-16}) 与 (3.55\times10^{-16})。后处理重跑后，两工况均写出 `qa_farfield_basis` 且 `suspect=False`，收集器在 `index.csv` 中保留该质量标记。配对结果汇总于 `Run/ch3_02_input_boundary/u2_paired_validation_report.json`，U2 判定通过，允许撰写第3.4节。

## U3a：网格收敛

### 工况与验收指标

U3a 固定均质基岩、坡高25 m、坡角30°、垂直入射、侧向净空6h、坡脚以下深度5h、1%阻尼、4 Hz Ricker 波和0.001 s最大时间增量，只把 CPE4R 单元尺寸依次设为12 m、8 m和6 m。三组 Abaqus 求解、ODB 后处理、NPZ 收集和统一绘图均由 `Batch/Autorun_ch3_03_mesh_convergence_v1.py` 串行完成，产物位于 `Run/ch3_03_numerical_convergence/run-001/`。

验收不只比较单个最大值，而是在固定501点归一化坐标上计算8 m曲线相对6 m参考曲线的 L2 相对差，同时比较两者 `AR_max` 相对差；二者均不超过5%才判为通过。12 m、8 m和6 m的 `AR_max` 分别为1.081579、1.079491和1.079491。8 m相对6 m的整曲线 L2 相对差为0.001338，即0.134%；峰值相对差为 \(1.77\times10^{-7}\)，即0.000018%。两项均远低于门槛，且三级工况的远场质量标记均通过。

### 判定与论文写入状态

U3a 判定通过。8 m网格已经达到本基准工况的空间离散无关性；继续细化至6 m显著增加自由度与求解时间，却没有带来可辨识的目标响应变化。因此后续 U3b—U6 的基准均匀网格取8 m，软弱覆盖层工况仍须由控制性最软层波长和穿层单元数另行约束，不能把本次均质基岩结论无条件外推。上述证据允许写入第3.5.1节；时间步、计算域和阻尼尚未完成，暂不写最终数值参数汇总。

## U3b：时间步收敛

U3b 首轮 `run-002` 暴露出两个流程问题。第一，主建模脚本中的 `time_cfg` 只负责时间步充分性诊断，实际分析步严格继承输入波文件的采样间隔；因此仅注入 `max_increment=0.002/0.0005` 并不会改变实际0.001 s增量。第二，在0.5 ms名义工况被人工终止后，原 U3b 控制脚本仍对未完成 ODB 执行后处理，并因三个工况实际上均为1 ms数据而错误给出收敛通过。`run-002` 因而整体判为无效证据，其报告不得进入论文。

控制脚本现已改为在每个工况目录内从原始4 Hz Ricker波生成真实的2.0 ms下采样记录和0.5 ms插值记录，并由 `run_cfg.wave_files` 注入；验证器还会读取 NPZ 的实际 `dt`，只有2.0、1.0和0.5 ms三档全部与目标一致才允许计算收敛指标。修复后的工况在 `Run/ch3_03_numerical_convergence/run-003/` 重新运行，U3a 的1.0 ms/8 m工况作为中间档复用。

## 2026-07-12：严格复核后的证据重定级

依据 `docs/技术文档/第三章严格审查与重构论证.md`，U0—U3b 的原始运行、失败日志和诊断结论全部保留，但不再沿用旧计划中的正式通过等级。重新判定如下：

| 历史单元 | 新等级 | 理由 |
| --- | --- | --- |
| U0 | `infrastructure_pass_only` | 证明四脚本链和 NPZ 数据链可运行，不证明有限元物理正确 |
| U1 | `single_case_diagnostic_pass_needs_expansion` | 只覆盖一个几何和分层组合，正式 V1 将扩为12例 |
| U2 | `directional_diagnostic_only_formal_validation_not_done` | 带坡体的上游 PGA 标量不能解耦输入、边界和坡体散射；正式验证需真正平坦解析/一维矩阵 |
| U3a | `easy_case_pretest_only` | 均质高波速4 Hz工况的三档网格均已很细，不能冻结软层和高频生产网格 |
| U3b | `frozen_pretest_run002_invalid_run003_unclosed` | run-002无效；run-003只覆盖易工况且报告未正式闭环，停止继续修复 |

旧正文中已经写入的成层文献复现、左右远场均小于2%、域尺寸扰动、阻尼和反射率等结论，不具有当前重构计划下的可追溯证据，已从新的活动工作稿移出。重构后正式证据从 F0/V1 开始编号，任务状态以 `docs/计划文档/第三章执行状态.json` 为唯一入口。

本次复核没有删除或覆盖 `Run/` 中任何旧产物，也没有继续运行 U3b。旧结果可用于回归和反例教学，但不得在新第三章中表述为正式方法验证结论。

重构后的活动 Word 为 `docs/论文材料/边坡地震动放大效应研究论文初稿（第三章重构）.docx`。该文件已通过 DOCX ZIP 完整性、OpenXML 可读性、第三/第四章边界、新 3.1—3.9 标题骨架和旧结论关键词扫描；原整合修订稿哈希保持不变。2026-07-12 已由本机 Microsoft Word 原生更新目录与域并导出 55 页 A4 校验 PDF，视觉复核确认第三章位于第26—27页、第四章从第28页开始，未发现标题截断、跨页错位或目录页码失效。Word 原生保存后的活动稿 SHA-256 为 `0e9af8a7e7887c5ac024689ec5cddd8db5dab12291b7c3af83d9ee67b42fccba`，校验 PDF SHA-256 为 `905be9f850ad0804e27e63322da5d63c9a36f13b3eaef1330b64819553e4f1ef`。

P0 已经研究者确认并完成闭环。正式工作流 Skill 安装于 `.agents/skills/chapter3-fe-validation-workflow/`，通过 `skill-creator` 官方校验器。研究者已于 2026-07-12 明确要求继续执行，`execution_hold` 已解除，当前进入 F0-1。F0-1 仍仅限于显式平场验证模式及静态回归，尚未授权正式验证求解。

## F0-1：显式平场验证模式建模回归（2026-07-12）

### 验证目的与实现范围

F0-1 的目标是把平坦场地从“用极小坡高近似”的替代方案改为 Hybrid v2 中可配置、可追溯的独立几何模式。新增 `run_cfg['validation_geometry']`，默认值为 `slope`，因此生产坡地仍沿用原 `create_model`、原文件名规则和原场景调度；只有显式注入 `flat` 且场景为 `freefield`、`submit_jobs=False` 时，才进入矩形平场建模函数。平场函数使用与坡地相同的材料带、阻尼、网格和 VAB 输入协议，将上下表面统一为坡顶高程，并写出 `geometry_validation.json` 与 `case_meta.json`。`submit_jobs=False` 仅用于 F0 基础设施回归，不代表有限元响应已经通过理论验证。

### 代表工况与结果

Autorun `Batch/Autorun_ch3_F0_01_flat_mode_v1.py` 在 `Run/ch3_F0_01_flat_mode/run-002/` 自动创建三类工况：

| 工况 | 几何/材料 | 节点/单元 | 顶面高程范围 | 结果 |
| --- | --- | ---: | ---: | --- |
| `case-flat-homogeneous` | 均质基岩、水平表面 | 490 / 442 | 0.0 m | 通过 |
| `case-flat-layered` | 两层成层、`surface_geometry=terrain`、平场退化 | 910 / 850 | 0.0 m；预期界面 160 m | 通过 |
| `case-slope-default-sentinel` | 默认坡地、均质基岩 | 360 / 315 | 50.0 m | 通过 |

三类工况的左、右、底和 `TOP_SURFACE` 节点集均为非空；平场工况的顶面 `y_range=0`，成层平场保留预期层界面记录，默认坡地哨兵的地表高差为 50 m，说明未误走平场分支。三类工况均未产生 ODB，Abaqus 求解进程为 0，未知锁文件为 0；该回归只确认几何/材料/集合生成和默认分支隔离，不确认输入波、人工边界或响应幅值准确性。

首次运行发现 Autorun 的 `next_run_dir()` 只计算编号而未创建目录，导致清单写入前失败；修复后在 `run-002` 重新运行，最终报告为 `status=passed`。该失败证据保留在前一次运行目录和日志中，未覆盖已有结果。

### F0-1 判定与后续支撑范围

F0-1 通过。它支撑后续 F0-2—F0-7 使用显式平场作为理论解和独立一维解的端到端载体，也支撑默认坡地生产模式没有因新增配置而改变。它不能替代 V2 的 Abaqus ODB—解析时程误差、V3 的成层传递函数对比、V4 的边界反射指标或 V5 的数值收敛验证；这些任务仍不得提前写入第三章正文。

## F0-2：独立 P–SV 层状参考解（2026-07-12）

### 独立性与输入输出

新增 `Modeling/Hybrid/reference_layered_psv_v1.py`，不导入 Abaqus，不导入 `slope_frame_ssi_full_v2.py`，也不调用生产 `_fd_solve_column()`。参考程序以半空间单位上行 SV 波为输入，在有限层中传播 P/SV 上、下行波，以状态向量 `[ux, uy, sigma_xy, sigma_yy]` 传递界面连续条件，并在顶部施加自由表面两类牵引为零。输入输出协议和门槛见 `docs/技术文档/独立P-SV层状参考程序输入输出说明.md`。

### 单元测试与固定基准

`test/Hybrid/test_reference_layered_psv_v1.py` 完成 5/5 测试：均质半空间垂直入射自由表面退化、同材有限层幅值退化、垂直入射 P–SV 到 SH Haskell 递推的幅值对拍、成层频率响应和 30° 斜入射自由表面牵引残差。测试不导入生产建模脚本。

`Batch/Autorun_ch3_F0_02_reference_v1.py` 以 4 类剖面（均质、低阻抗差单层、软层单层、双层）×3 个入射角（0°、15°、30°）×3 个频率（2、4、8 Hz）生成 36 条固定记录，直接写入 `Run/ch3_F0_02_reference/run-002/reference_transfer.npz`，并写出 `reference_manifest.json` 与 `f0_2_validation_report.json`。所有记录有限性检查通过，最大自由表面牵引残差为 `6.421671246070561e-13`；均质半空间垂直入射的 |ux|Vs 比值为 `[2.0, 2.0, 2.0]`。

首次运行发现基准汇总中的 `all(np.isfinite(...))` 对 NumPy 数组直接求真值会报“数组真值不明确”；该问题只发生在 Autorun 聚合逻辑，参考解本身未失败。修复后在 `run-002` 重跑通过，失败的 `run-001` 及其配置/日志均保留。

### F0-2 判定与后续支撑范围

F0-2 通过。该结果提供了不依赖生产 FD 内核的频域参考基准，可用于 F0-3 的理论时程/频谱字段设计以及 V2/V3 的平坦均质、平坦成层端到端对比。它尚未证明 Abaqus ODB 与参考解一致，也不能替代人工边界反射、网格/时间步或独立二维方法验证；当前任务切换至 F0-3。

## F0-3：原始时程与 NPZ QA 扩展协议（2026-07-12）

### 协议改动

`Postprocess/Postprocess_All_surface_v2.py` 在保留原有 `table_*`、`surface_summary_json` 和 `manifest_json` 键的基础上，新增 `raw_<record>_*` 命名空间。每条 ODB 记录现在可以直接写入 `time`、`x`、`y`、全地表 `acc_h/acc_v`、输入时程、理论基线时程、代表点误差数组、代表点索引和 `energy_time`。当前并列写入`qa_theory_json`、`qa_reflection_json`、`qa_energy_json`、`qa_frf_json`和`qa_response_spectrum_json`五个质量字段；后两项默认只记录，生产配置显式列入`run_cfg.qa_cfg.required`后才进入总体门槛。

当前理论时程字段明确标记为 `baseline_only`，来源是自由表面系数乘以输入波；它不是成层精确参考解，后续 V2/V3 应用 F0-2 独立参考包替换/补充。反射字段当前明确为 `not_computed`，能量字段明确为 `not_available_in_current_freefield_output`，避免把缺失证据伪装成通过。

### 协议测试

新增 `test/Hybrid/test_npz_protocol_v2.py`，在屏蔽 Abaqus 模块后验证理论基线、幅值误差、目标频率相位误差、代表点二维时程和三个 QA JSON 均可直接写入压缩 NPZ；测试结果为 4/4。`Postprocess_All_surface_v2.py` 与测试脚本均通过 Python 语法检查。该阶段尚未重跑 ODB，因此没有把协议测试误写成物理验证结果。

### F0-3 判定与后续支撑范围

F0-3 通过。NPZ 已具备后续理论/反射/能量 QA 分栏所需的数据承载能力，旧收集器可继续读取原有表键。下一任务为 F0-4：拆分 `qa_theory`、`qa_reflection`、`qa_mesh`、`qa_time`、`qa_domain`、`qa_energy` 和 `qa_external`，并禁止任何单项覆盖其他失败。

## F0-4：并列 QA 门槛与整体通过逻辑（2026-07-12）

### 改造目的

F0-4 用于修复旧后处理把多个物理问题压缩成单一 `suspect` 的逻辑缺陷。端点远场对拍只能回答理论基线/输入一致性问题，不能代表人工边界反射、网格、时间窗、计算域、能量或外部基准均已通过。因此新增 `QA_GATE_NAMES` 和 `evaluate_qa_gates()`，将 `theory`、`reflection`、`mesh`、`time`、`domain`、`energy`、`external` 作为并列门槛保存，并由 `qa_cfg.required` 明确本任务的强制子集；未提供证据的门槛返回 `False`，不适用项不能被默认为通过。

### 判定规则与最小证据

`qa_theory` 仅在具有有效传播方向基准端点误差且 `suspect=False` 时通过；`qa_reflection` 和 `qa_energy` 分别只接受 NPZ 中对应 JSON 状态为 `passed`，当前 F0-3 的 `not_computed` 与 `not_available_in_current_freefield_output` 因而仍明确失败。`qa_time` 优先读取窗口收敛结果，`qa_mesh`、`qa_domain` 和 `qa_external` 必须由汇总记录显式给出。最终结果满足

\[
\mathrm{overall\_pass}=\bigwedge_{q\in\mathrm{required}} q,
\]

并同时保存 `qa_gates`、`qa_gate_status`、`qa_required`、`qa_status` 和 `qa_farfield_endpoint_suspect`，使“理论端点诊断”和“整体可信性”可以独立审计。

### 窗口收敛覆盖缺陷的修复

`apply_window_convergence_qa()` 不再执行 `rec['suspect']=not passed`，也不再用窗口收敛结果删除端点诊断。它只写入 `qa_time_pass` 与 `qa_window_convergence`，随后重新调用整体评估器；因此窗口通过不能覆盖反射或能量失败，窗口失败也不会伪造新的端点反射结论。

### 回归与负对照

`test/Hybrid/test_npz_protocol_v2.py` 扩展为 7/7：首先验证原始时程、误差数组和 QA JSON 可以直接写入 NPZ；随后构造 `suspect=False` 但反射/能量状态未就绪的记录，确认 `qa_theory=True` 而 `overall_pass=False`；再将强制列表缩小为 `['theory']`，确认仅理论子问题可以独立通过；最后构造端点理论失败并保持时间门槛为真，确认失败不会被其他门槛覆盖。`Postprocess_All_surface_v2.py` 及三项 F0 纯 Python 回归均通过：NPZ 协议 7/7、平场几何 12/12、独立 P–SV 参考 5/5。

### F0-4 判定与支撑范围

F0-4 通过。该任务证明 QA 逻辑具备失效检出和证据隔离能力，允许 F0-5 将这些状态展开到统一收集索引；它不表示当前模型已经通过理论、反射或能量验证，因为反射和能量字段仍分别处于 `not_computed` 与 `not_available` 状态。下一任务为 F0-5：扩展 `Collect_All_results_v2.py` 的实际配置和审计字段。

## F0-5：Collect 实际审计字段展开（2026-07-12）

### 改造内容

`Postprocess/Collect_All_results_v2.py` 继续以 `surface_results.npz` 为主数据源，同时读取同一工况的 `case_meta.json`、`case_config.json`、`geometry_validation.json` 和建模日志。索引保存实际时间步与时长、地表节点数、模型节点数、单元数、单元类型、阻尼开关/中心频率/方法/锚定方式、域包络及其来源、验证几何模式、四个固定脚本哈希、输入波哈希，以及既有七类QA和新增FRF/反应谱两类QA字段。模型节点/单元优先采用几何审计，旧工况则回退到建模日志；域包络优先采用几何审计，缺失时明确标记为 `case_meta.geometry_fallback`，避免把估算值冒充 ODB 实测值。

### 回归证据

新增 `test/Hybrid/test_collect_audit_v2.py`，构造带 `surface_summary_json`、几何审计和网格日志的最小 NPZ 工况，验证 `actual_dt=0.001`、模型节点 `490`、单元 `442`、域上限 `270 m`、`qa_theory=True`、`overall_pass=True` 及脚本哈希字段均能展开，结果为 5/5。随后对既有 `Run/ch3_00_smoke/run-005/case-homogeneous-freefield/surface_results.npz` 运行收集器，输出 `test/Abaqus/f0_5_collect_qa2/index.csv`；表头和数据行均包含新增审计列，旧 NPZ 的兼容收集成功。该既有工况的旧摘要没有 F0-4 门控字段，因此 QA 列留空，不能被误判为新门槛通过。

### F0-5 判定与支撑范围

F0-5 通过。统一索引现在可以独立审计“实际产物—配置—脚本版本—输入波—QA 状态”的对应关系，支持后续 V1—V9 的数据追溯；它没有补造旧工况缺失的能量或反射证据。下一任务为 F0-6：在纯 freefield 输出中补齐能量历史，并据此计算人工能量比和相对能量残差。

## F0-6：freefield 能量历史与能量 QA（2026-07-12）

### 输出改造与工况

在 `slope_frame_ssi_full_v2.py` 的 freefield 输出函数中新增整体历史请求 `H-Energy`，变量为 `ALLAE`、`ALLIE`、`ALLKE`、`ALLVD`、`ALLWK`、`ALLPD`、`ALLSE` 和 `ETOTAL`。`Postprocess_All_surface_v2.py` 从最后一个动力步的整体历史区域提取这些序列，按统一时间轴写入 `raw_<record>_energy_*` 和 `raw_<record>_energy_time`，并把人工能量比、能量残差、阈值、缺失变量及状态写入 `qa_energy_json`。能量 QA 不依赖 CSV，且缺少变量时返回 `not_available`，不伪造通过。

F0-6 只登记一个真正平坦、均质、垂直入射的 freefield 控制工况，目录为 `Run/ch3_F0_06_energy/run-001/case-flat-homogeneous-energy/`。模型采用 CPE4R、8 m 网格、0.001 s 实际增量、2 s Ricker 波和 1 s 静默尾段；`qa_cfg.required` 仅包含 `energy`，因此本任务只回答能量/人工误差问题，不把尚未完成的反射、网格或外部基准门槛混入本次判定。完整链路仍依次执行建模、Abaqus 求解、ODB 后处理、NPZ 收集和绘图。

### 实际结果

ODB 完成标志为 `THE ANALYSIS HAS COMPLETED SUCCESSFULLY`，无新增锁文件或活动 `standard` 进程。NPZ 中保存 9 个能量相关键（时间轴加 8 个变量），`qa_energy_json` 给出：人工能量比

\[
\eta_{AE}=\max|ALLAE|/\max|ALLIE|=4.4131\times10^{-7},
\]

相对能量残差

\[
\eta_E=\max|ETOTAL|/\max|ALLWK|=3.2500\times10^{-6}.
\]

二者均低于预登记的 0.05 门槛，能量状态为 `passed`，F0-6 验收报告为 `Run/ch3_F0_06_energy/run-001/f0_6_validation_report.json`。纯 Python 回归也通过：能量计算/缺失变量/NPZ 写入 4/4，NPZ 协议 7/7，收集器审计 5/5。

### F0-6 判定与支撑范围

F0-6 通过。该结果支撑 CPE4R freefield 控制工况的人工能量和整体能量平衡处于可接受数值误差范围，并证明能量字段可以随原始时程直接进入 NPZ 和统一索引；它不能替代多网格、多时间步、多域尺寸和坡体散射工况的能量敏感性。下一任务为 F0-7：给 Autorun 增加清单哈希一致性门禁，防止不同脚本版本和配置被混合收集。

## F0-7：Autorun 清单与脚本/输入哈希门禁（2026-07-12）

新增 `Batch/Autorun_ch3_F0_07_manifest_hash_v1.py`，将运行清单中的 `source_sha256`、`reference_sha256`、`reference_script` 和 `run_dir` 转换为可执行的一致性检查。它不按目录名猜测“最新 run”，也不把 `run-001` 写成隐含基线；调用时必须显式传入要审计的 manifest，文件路径和哈希均由清单解析。校验范围包括 F0-1 建模/输入波、F0-2 独立参考程序和 F0-6 四脚本链/输入波。

对 `Run/ch3_F0_01_flat_mode/run-003/f0_1_run_manifest.json`、`Run/ch3_F0_02_reference/run-002/reference_manifest.json` 和 `Run/ch3_F0_06_energy/run-001/f0_6_run_manifest.json` 的三份实际清单核验全部通过，报告写入 `Run/ch3_F0_01_flat_mode/run-003/f0_7_manifest_hash_report.json`。`test/Hybrid/test_manifest_hash_gate_v2.py` 进一步把独立参考程序哈希改写为 64 个零并执行负对照，验证不一致会抛出错误；测试结果 2/2。第一次控制台归档命令误把报告路径假设在 F0-6 目录，属于输出路径检查错误，未改变清单校验结果，修正后报告位置已固定。

F0-7 通过。后续 Autorun 必须先产生并锁定清单，再按清单调用固定四脚本；基线参考、配置、输入波和结果不能通过硬编码旧 `run-001` 混用。下一任务为 F0-8：检查 EQL/非线性生产模式的失败闭锁。

## F0-8：EQL/非线性失败闭锁（2026-07-12）

### 风险与改造

旧 `_run_eql_if_enabled()` 在 EQL 计算异常时记录“回退线性”并继续建模。这会使名义非线性工况悄然变成线性工况，尤其会污染关于强震放大削弱和机器学习标签的解释。现将 `eql_cfg['fail_closed']` 默认设为 `True`，异常时写明原因并抛出 `RuntimeError`；只有在配置中明确设置 `fail_closed=False` 时，才允许作为诊断模式回退线性。

### 测试与回归

`test/Hybrid/test_eql_fail_closed_v2.py` 用 Abaqus 模块桩和合成 EQL 异常执行两条路径：闭锁开启时必须抛出包含 `fail_closed=True` 的错误，闭锁显式关闭时才返回 `{'enable': False}`，结果为 2/2。主建模脚本修改后，`Batch/Autorun_ch3_F0_01_flat_mode_v1.py` 重新完成三工况只建模回归，产物位于 `Run/ch3_F0_01_flat_mode/run-004/`，均质平场、成层平场和默认坡地哨兵的几何审计全部通过且未产生 ODB。随后使用改进后的递归本地脚本查找规则重新核验 F0-1(run-004)、F0-2(run-002)和 F0-6(run-001)清单，哈希一致性通过；历史 run 的本地脚本版本不会被当前仓库版本错误替换。

### F0-8 判定与支撑范围

F0-8 通过。该门禁保证论文生产 EQL/非线性工况不会在失败后静默退回线性；它不等于已经完成非线性响应验证，也不改变当前第三章以线性机制为主的结论边界。F0 全部通过，下一任务进入 V1：12 个代表性几何、材料和观测生成检查工况。

## V1：几何、材料与观测生成矩阵（2026-07-12）

### 工况设计

V1 不提交地震求解，只检验参数化生成器是否按配置建立几何、材料带、观测面和边界节点集。12 个工况覆盖坡高 25/50/100 m、坡角 30°/45°/60°、均质/单层/双层、水平/沿坡等厚分层、2 m 薄软层、同材人工分层以及水平分层组合；每例采用 `validation_geometry=slope`、CPE4R 和 8 m 网格，并保留统一 `TOP_SURFACE`、左右/底部边界和 `CREST_REF` 生成逻辑。

### 审计项目与结果

Autorun `Batch/Autorun_ch3_V1_geometry_material_v1.py` 在 `Run/ch3_V1_geometry_material/run-001/` 串行建模。每例检查 `geometry_validation.json`、`case_meta.json` 和实际目录：坡面地表高差大于 1 m；节点/单元数为正；`Left_boundary`、`Right_boundary`、`Bottom_boundary`、`TOP_SURFACE` 均非空；`H_minus_h` 与注入坡高一致；坡角、表层几何和有限层数与配置一致；未生成 ODB。12/12 工况通过，模型节点数为 206—2967，单元数为 178—2860，说明极小/中值/大尺度和多层分区均能落地。V1 清单哈希核验同时通过，报告保存在同一 run 目录的 `v1_hash_console.txt` 和 `f0_7_manifest_hash_report.json`。

### V1 判定与支撑范围

V1 通过。该结果支撑几何、材料和观测生成器可按预登记组合建立，并允许进入 V2 的真正平坦均质 ODB—解析对比；它不支撑任何响应幅值、人工边界反射、网格收敛或坡地放大结论。下一任务为 V2：15 个入射角/频率组合的平坦均质半空间解析端到端验证。

## V2：平坦均质半空间解析端到端 pilot（2026-07-12，等待决策）

### 执行与首次问题

V2 Autorun `Batch/Autorun_ch3_V2_flat_homogeneous_v1.py` 已建立 5 个入射角（−30°、−15°、0°、15°、30°）×3 个频率（2、4、8 Hz）的 15 例矩阵入口，并在正式矩阵前执行同一 pilot（−30°、2 Hz）。首轮 `run-001` 暴露出 Autorun 自生成 Ricker 子波中心时刻错误：`t0=0.5/f` 使初值约 −0.33，产生非物理启动脉冲；该失败 run 保留。

将中心时刻改为 `t0=2/f` 后，`run-002` 的输入起始行为恢复正常，但 450 m 平场域对于 2 Hz、Vs=2000 m/s 的约 1000 m 波长仍过窄，出现 20—40% 全时程偏差。随后把平场观测窗扩至约 20h，域宽增至 1125 m，形成 `run-003`。该 run 的地表/地下双分量时程、能量历史、NPZ 和图件均成功生成；以相位随 x 的直达波修正和主脉冲窗口离线检查时，中心区域 NRMSE 约 2.3%，但正式冻结的全时程指标仍未通过，端点和尾段受边界/相位原点影响明显。

### 当前判定

V2 暂不判定通过，也不启动剩余 14 例。失败证据、报告和 ODB 分别保留在 `Run/ch3_V2_flat_homogeneous/run-001/`、`run-002/`、`run-003/`，汇总决策项为 `Run/ch3_V2_flat_homogeneous/v2_pilot_diagnostic_report.json`。在继续前必须由研究者冻结：解析参考相对于 FE 输入边界的相位原点、直达波/反射波比较窗，以及三处地表点是否应排除人工边界端点。冻结后才可修改 V2 Autorun 并用同一 pilot 回归；不得事后放宽 NRMSE、峰值、相位阈值或直接把主脉冲窗口结果写成全时程通过。

## V2 协议冻结与 pilot 重跑登记（2026-07-13）

研究者确认采用“预注册直达波窗口 + 明确相位原点”的 V2 方案。该方案回答的是“二维有限元模型能否在目标频带内正确传递斜入射波的到时、主要能量包络、峰值和频谱特征”，不要求受后期人工边界反射影响的全时程逐点相同。输入 Ricker 波主峰中心固定为 \(t_0=2/f_c\)，比较窗固定为 \([t_0-2T_c,\ t_0+4T_c]\)，其中 \(T_c=1/f_c\)。

地表测点按观测面实际 x 范围的 0.25、0.50 和 0.75 分位选取，禁止使用最靠近左右边界的节点。理论时程采用独立 P–SV 参考程序，并显式加入模型域中点水平相位原点以及 `base_depth×slope_height` 参考深度对应的传播时延；窗口内时移由最大归一化互相关自动确定，不使用人工调 delay。四项冻结门槛为：到时误差不超过 \(0.02T_c\)、峰值相对误差不超过 5%、窗口 NRMSE 不超过 10%、主频幅值谱误差不超过 5%。相位误差只作为辅助诊断量，不替代四项主门槛。

为避免 2 Hz 工况的 3 s 比较窗被侧边界反射污染，本轮 pilot 将平场域观测窗扩至 300h（总宽约 15 km），保留同一有限元单元、材料、输入波和后处理链，仅改变预登记的域尺寸与比较协议。首先只执行 −30°/2 Hz pilot；pilot 的 `v2_validation_report.json`、NPZ、ODB、原始图和失败/修复日志全部保留。只有 pilot 四项主门槛在三个内部地表点均通过，才允许扩展 −30°、−15°、0°、15°、30° × 2、4、8 Hz 的 15 例矩阵。未通过时只诊断最早异常，不写入论文正文。

### run-004/run-005 诊断与下一次预登记

run-004（域宽约 6 km、左端相位原点）在右侧测点出现有限记录的起始脉冲，判定为相位原点与有限输入时程不一致；该 run 不作为正式证据。run-005 将 FE 与理论相位原点统一到域中点，三个测点的启动脉冲消失，到时、峰值和主频谱指标基本稳定，但 3 s 窗后段 NRMSE 仍为 10.0%—47.5%，说明 6 km 域的侧边影响仍进入冻结直达波窗。该结果不通过 V2，也不允许放宽 NRMSE 或缩短比较窗。

下一次 pilot 预登记把 `crest_window` 和 `toe_window` 均设为 300h（总宽约 15 km），使 x/L=0.25 与 0.75 测点到相应侧边的斜入射水平传播距离超过 3 s 比较窗；其余材料、网格、输入波、相位原点、指标和阈值全部保持不变。若 run-006 仍不能通过，停止继续扩大域尺寸，转入“人工边界/有限域模型与该解析参考的适用性不一致”方法学决策，不得无限制试算。

run-006 证明 300h 域可以降低侧边反射，但域中点相位原点下最大水平相位提前量约 1.9 s，超过原始 Ricker 记录的 1 s 预峰时长，右侧测点仍出现起始截断。为保持三点均有完整直达波，下一次最小修改仅对输入波增加预注册预滚：

\[
t_{\rm pre}=|p|L/2+0.25\,\mathrm{s},\qquad p=\sin(30^\circ)/V_s,
\]

其中 (L) 为平场域宽度；记录中的实际主峰为 (t_{0,\mathrm{record}}=t_{\rm pre}+2/f_c)，比较窗按该时刻平移，四项阈值、空间点、相位原点和 FE 网格全部不变。该修改不通过结果反推，只修正有限记录的输入定义；若预滚后的 run-007 仍失败，停止继续扩大域或调窗，形成 V2 方法学决策。

### run-007 结果与 V2 停止判定

run-007 按预滚协议生成了约 895 MB 输入文件和约 6.4 s 分析时程，Abaqus ODB/状态文件持续写入但在 Autorun 的 1 h 上限内仅推进到约 5.28 s，未完成正常关闭、后处理和 NPZ 验收。求解器与 CAE 子进程随后按超时处置终止，原始 ODB、`.sta/.msg/.stt`、输入文件及 `.lck.stale` 均保留。该结果不是“通过”或“失败指标”，而是当前 15 km 域+预滚验证方案的计算预算失败证据。

综合 run-004—run-007，V2 已完成相位原点、域尺寸和有限记录定义的最小可证伪修复，但没有形成三个地表点四项指标同时通过的证据。按照停止条件，暂停 V2 求解，不启动 15 例矩阵，也不把任何 V2 pilot 结果写入论文正文。当前唯一决策项为：将 V2 解析端到端对拍降级为诊断证据并转入 V3/V4 数值收敛验证，或由研究者另行批准更高计算预算/更紧凑的独立参考模型。

## F1：频域理论验证重构登记（2026-07-14）

### 方法学决策

依据 V2 pilot 的完整诊断证据，V2 不再作为第三章核心的时域逐点解析验证，也不启动原定15例矩阵。其全部运行目录、失败指标和超时残片继续保留，作为“斜入射时域验证方法探索与误差来源分析”的技术记录。当前核心路线改为比较输入—输出频域传递函数，使验证目标直接对应后续坡地放大分析中的频率响应、目标频率增益和主频位置。

### 预注册工况与数据链

F1 使用 `Batch/Autorun_ch3_F1_frequency_theory_v1.py`，固定调用 `slope_frame_ssi_full_v2.py`、`Postprocess_All_surface_v2.py`、`Collect_All_results_v2.py` 和 `Plot_Hybrid_surface_v2.py`，并复制独立参考程序 `reference_layered_psv_v1.py`。正式计算前登记5个代表工况：A1（0°、均质、2 Hz）、A2（15°、均质、2 Hz）、A3（30°、均质、2 Hz）、A4（0°、20 m软层覆盖基岩、2 Hz）和 A5（15°、同一成层模型、4 Hz）。每例在地表观测范围的 (x/L=0.25、0.50、0.75) 三个内部位置取样，避免把人工边界节点作为理论对拍点。

Autorun 从 NPZ 中读取输入和地表原始时程，计算 (H_{FE}(f)=U_{surface}(f)/U_{input}(f))，并在 `[0.5fc, 1.5fc]` 目标频带内与独立参考传递函数比较。预注册主指标为：频带幅值传递函数 NRMSE≤5%、(f_c) 处增益相对误差≤5%、输出主频相对误差≤3%；复传递函数相位只用于传播延迟和方向性诊断，不作为逐点时程通过条件。每个工况必须三个测点同时满足三项主门槛，才可进入下一任务。

### 当前状态与门禁

F1 已完成协议、工况清单、脚本入口和纯 Python 合成正负对照测试（2/2），并在研究者最新授权后完成 A1 pilot；A1 的失败 run 和修正 run 均保留，当前下一项任务为 A2。A1 通过只证明均质垂直入射控制工况的频域传递函数实现可靠，不能替代成层、斜入射或坡地散射证据。

### A1 pilot：失败诊断、最小修正与通过结果

研究者在 2026-07-14 明确要求继续执行第三章后，F1 A1（0°、均质基岩、2 Hz）按固定四脚本链完成了三轮可追溯运行。`run-001` 采用 100 m 底部深度和 8 m 网格，水平频带 NRMSE 为约 8.4%，且垂直分量理论值接近零，暴露出底部反射影响和零分量相对误差分母失效；该目录保留为失败诊断。`run-002` 将底部深度增至20h、网格改为16 m后，时域峰值仍接近理论值，但直接用不同时间步的输入和输出数组做 FFT 产生约45%的假增益；该目录同样保留。`run-003` 在相同几何基础上采用标准 Liu 弹簧恢复系数 `spring_scale=2.0`，并修正频域处理：将 NPZ 中 1.000 ms 输入波插值到 ODB 实际 0.9999275 ms 输出时间轴，使用完整矩形记录窗比较传递函数幅值；垂直入射下理论为零的垂直分量改为只报告数值泄漏，不计入幅值相对误差门槛。

`run-003` 的 ODB 正常完成，NPZ、收集索引、统一图件和频域专用图表均生成。三个内部地表点（x/L=0.25、0.50、0.75）的水平传递函数幅值 NRMSE 分别为 0.285%、0.324% 和 0.254%，目标频率增益误差分别为 0.130%、0.125% 和 0.096%，输出主频相对误差均为0；垂直分量最大数值泄漏增益为 1.05%，未作为理论非零分量验收项。三点均满足 F1 预登记的5%、5%和3%门槛，A1 判定通过。报告、指标表和图件分别保存在 `Run/ch3_F1_frequency_theory/run-003/f1_validation_report.json`、`F1_A1_metrics.csv` 和 `F1_A1_transfer_function.png/pdf`。该结果只证明当前平场均质控制工况在指定频带和时间轴协议下的传递函数实现可靠，不能替代成层工况、斜入射工况或坡地散射验证；下一任务为 A2（15°、均质、2 Hz）。

### A2—A5 完成与 F1 总体判定

A2（15°、均质、2 Hz）和 A3（30°、均质、2 Hz）均按与 A1 相同的 20h 底部深度、16 m 网格、标准 Liu 弹簧系数和时间轴插值协议完成。A2 三测点水平/竖向频带 NRMSE 最大值分别为 0.373%/0.849%，目标增益误差最大值分别为 0.285%/1.062%；A3 对应最大值为 0.357%/0.689% 和 0.327%/0.921%，主频均通过。A4（0°、20 m软层、2 Hz）首轮 `run-006` 因 layer 配置缺少生产脚本所需字段而失败，修正为同时保留建模字段（`name、poisson_ratio、density`）和独立参考字段（`rho、nu`）后，`run-007` 正常完成。A4 改用独立 SH 层状传递函数处理垂直入射退化，三点水平频带 NRMSE 为 0.374%–0.450%，目标增益误差为 0.149%–0.180%，通过；`run-006` 作为配置失败原始证据保留。

A5（15°、20 m软层、4 Hz）首轮 `run-008` 已完成求解但使用16 m网格时竖向分量单独峰频误差偏大。随后将4 Hz工况网格细化至8 m，`run-009` 的水平频带 NRMSE 为 0.360%–0.840%，竖向频带 NRMSE 为 1.298%–1.949%，两分量目标增益误差均低于0.8%。由于斜入射成层问题的竖向谱可能出现局部峰分裂，最终主频门槛采用合成传递谱（|H_h|²+|H_v|² 的平方根）评价，三测点合成峰频误差均为0；但竖向单分量峰频误差最大为9.37%，超过3%线。A5因此按“幅值、增益、水平及合成峰频通过，竖向分量峰频未通过”的条件通过报告，`run-008` 保留为网格/指标诊断证据。

因此 F1 中A1—A4完全通过，A5条件通过：均质介质覆盖0°、15°、30°，成层介质覆盖0°、15°和2/4 Hz，三处内部测点均参与验收。F1支撑第3.5.2—3.5.3节写入“频域理论对比”和“平坦成层独立解对比”的限定性结论，但不支撑坡地散射、网格/时间步收敛或跨求解器交叉验证。F1完成后转入V3坡地数值解验证。

## 第三章 Word 工作稿写入与视觉复核（2026-07-14）

依据 F1 A1—A5 的正式报告，已将第三章重构内容写入 `docs/论文材料/边坡地震动放大效应研究论文初稿（第三章重构）.docx`。正文已覆盖 3.1—3.9 的方法、工况、指标、结果边界和可复现性说明；3.5.2 内嵌 A1 传递函数图并配图题。对尚未执行的 V3—V7，正文保留“待验证”状态和不得提前外推的限制，不将计划矩阵写成已通过结果。

由于标准渲染脚本缺少 `pdf2image` 依赖，本轮使用 LibreOffice 无界面转换和 Poppler 逐页栅格化完成视觉检查。检查确认第三章连续排版、A1 图形未被分页截断、公式已改为正文可读文本，第四章从第35页开始。最终渲染产物保存在 `Run/ch3_docx_render_20260714_fix/`；后续每次修改 Word 后仍需重新执行同一渲染检查。

## V3正式坡地数值解验证重启登记（2026-07-16）

本轮首先在 `Run/ch3_V3_slope_solution_verification/run-001/` 启动均质基岩坡地矩阵，S0和M1正常完成，M3运行中止。复核发现该矩阵采用 $V_s=2000$ m/s、4 Hz输入和8 m中档网格，在目标频带内已远高于每波长10个单元，仍存在与旧U3a相同的“高波速易工况对控制性软层缺乏约束力”问题。为避免把弱证据包装为正式收敛结论，run-001保留为诊断记录，不进入论文指标表；中止不是模型失败，也不作通过判定。

正式矩阵改在 `run-002/` 执行。控制工况为坡高25 m、坡角30°、15°斜入射、4 Hz Ricker波，设置20 m厚随地形软覆盖层；覆盖层 $V_s=800$ m/s、密度2200 kg/m³，基岩 $V_s=2000$ m/s、密度2500 kg/m³，泊松比均为0.30。按程序诊断的10 Hz有效最高频率，最短剪切波长为80 m，12/8/5.33 m三级网格分别对应6.7/10.0/15.0个单元/最短波长，使中档网格位于工程控制线而细档提供独立参照。时间步采用2.0/1.0/0.5 ms三级，侧向净空采用4h/6h/8h三级，底部深度采用3h/5h/7h三级；另设H0均质无界面与L1同材人工分层对照。全部11个工况调用当前 `Modeling/slope_frame_ssi_full_v2.py` 和 `Postprocess/Postprocess_All_surface_v2.py`，在501点固定 $s$ 坐标上比较完整TAF曲线、峰值与峰位。

正式通过门槛在求解前冻结：中档相对细档或远域的整曲线L2相对差和峰值相对差均不超过2%，峰值位置偏移不超过0.02h；真实ODB时间步必须与2.0/1.0/0.5 ms目标一致，且所有工况的既有质量标记不得为可疑。M1、T1、D1和B1粗档只用于显示趋势，不承担通过判定。当前 `run-002` 已启动，结果未完成前不得在正文写成“通过”。

## V4文献坡地外部基准预登记（2026-07-16）

V4复现Shen等（2024）验证算例2：200 m高、45°台阶坡，15°斜入射，8 Hz且峰值1 m/s²的Ricker加速度输入，材料参数为 $E=20$ GPa、$\nu=0.30$、$\rho=2500$ kg/m³。几何总长8h、总厚3h、坡肩后方距离3h；本文采用8 m CPE4R网格和0.5 ms输入采样。外部参考为原文图9中标注“This study”的ABAQUS红/绿实线，原文已用SPECFEM2D虚线交叉验证。本文从本地论文图像逐像素数字化公开实线，但不把数字化结果冒充作者原始数组或SPECFEM2D原始数据。

V4比较坡肩A和坡脚B的水平、竖向4通道时程。只允许由四通道合计归一化相关确定一个不超过0.15 s的公共时间平移，不允许逐通道平移、幅值缩放或符号翻转；在0.4—0.9 s窗内，四通道波形NRMSE和绝对峰值误差均不超过15%才判为通过。15%阈值在求解前冻结并包含JPEG像素化、线宽及坐标标定误差。响应门槛还必须与既有5%人工能量比和能量残差门槛同时通过，任何一项失败均不得由其他指标覆盖。V4将在V3完成后串行求解，避免Abaqus资源争用。

### V4首轮失败诊断与文献系数复算

首轮动力分析正常完成，人工能量占比与能量残差分别为 $4.31\times10^{-6}$ 和 $5.05\times10^{-6}$，但四通道响应门槛未整体通过。首先发现数字化脚本为避开图例而误删了0—约0.47 s的整段像素列，再以首个有效值外推，造成原论文图中不存在的矩形平台；该失败报告已保存为 `v4_validation_report_v1_digitization_failed.json`。修正为仅屏蔽图例实际占据的像素行后，坡脚水平通道NRMSE由41.4%降至22.7%，证明首轮参考数组确有提取伪差，但修正后的四通道仍未全部通过冻结门槛。

继续核对原文发现，首轮模型沿用了本项目正式参数研究的通用边界缩放，而Shen等[7]式（7）明确采用 $a=0.8$、$b=1.1$，其法向与切向弹簧系数不能由一个共同缩放量精确表示。生产建模脚本因而增加了可选的法向/切向弹簧与阻尼缩放键，缺省值仍回退到原有共同系数，不改变任何既有工况。V4 `run-002` 将其精确换算为法向弹簧1.94444、切向弹簧1.11111、双向阻尼1.1后重新求解。该修改来自公开公式核对，不改变既定比较窗、公共时移规则或15%响应门槛；首轮结果继续作为边界配置不一致的失败证据保留。

### V4最终结果与停止判定（2026-07-17）

`run-002` 动力分析与后处理正常闭合，四通道共同时间平移为−0.0655 s，平均归一化相关系数为0.899。坡肩A水平通道的波形/峰值误差为15.71%/7.72%，坡肩A竖向为13.96%/13.31%，坡脚B水平为22.62%/3.56%，坡脚B竖向为105.88%/17.47%。仅坡肩A竖向同时满足两项15%门槛，`four_channel_response=false`；人工能量占比和能量残差分别为 $4.27\times10^{-6}$ 和 $4.63\times10^{-6}$，`energy=true`。总体逻辑合取结果为 `status=failed`。

边界系数精确换算相对修正数字化后的首轮结果变化很小，继续调节边界系数不再具有文献依据；逐通道移相、幅值缩放、缩短比较窗或放宽阈值均违反冻结协议。因此V4在 `run-002` 后停止，不再试算。其结果可作为“主脉冲峰值和坡肩响应具有一定一致性”的诊断证据，但不得写成外部验证通过。后续闭环需要作者原始数组或独立SPECFEM2D计算，而不是继续从JPEG反推参数。

## V3第一版峰位门槛失败与第二版协议冻结（2026-07-16）

`run-002` 首批9组求解与后处理全部闭合，但第一版自动报告为 `failed`，失败文件原样归档为 `v3_validation_report_v1_failed.json`（SHA-256：`8E798F6984354ADBAB6185D04DA3A4BC7B26C3755B57A9DE46265F9CB0B058F6`），运行日志同时保留。第一版中，网格、时间步、侧向域和同材退化的整曲线L2相对差分别为0.488%、0.013%、0.197%和0.156%，峰值相对差分别为1.327%、0.013%、0.061%和0.308%，均满足事前2%门槛；三档实际时间步与目标一致，9组 `suspect` 均为 `false`。失败仅来自单点argmax位置：S0/M3相差0.211h，H0/L1相差0.640h，超过0.02h。

进一步检查完整曲线表明，H0和L1的99%近峰区分别覆盖约 $-1.62\le s\le0.16$ 与 $-1.71\le s\le0.18$，两条曲线L2仅差0.156%，但宽缓峰台上的最高离散点分别落在 $s=-0.96$ 与 $s=-0.32$。因此，单点argmax把近乎等高节点的数值排序误当成稳定物理位置，不适合作为宽峰曲线的强制通过量。该诊断不删除第一版失败，也不放宽曲线或峰值2%门槛。

第二版协议在补算底部深度B1/B3之前冻结：继续要求整曲线L2和峰值差均不超过2%，空间稳定性改为99%近峰区重叠率不小于0.80；原单点argmax偏移继续完整报告并按0.02h给出诊断真假，但不再参与总体逻辑合取。第二版报告采用 `schema_version=2`，明确保存 `checks` 与 `diagnostics` 两组字段，使“正式目标量通过”和“更严格但不适定的单点诊断失败”不能混淆。底部深度3h/5h/7h三档仍按同一第二版门槛比较，完成前不得写入正文结论。

## V3正式结果（2026-07-16）

`run-002` 的11个工况均正常完成，最终 `v3_validation_report.json` 为 `schema_version=2`、`status=passed`。S0相对M3、T3、D3和B3的整曲线L2相对差依次为0.488%、0.013%、0.197%和0.200%，峰值相对差依次为1.327%、0.013%、0.061%和0.033%，99%近峰区重叠率依次为0.933、1.000、1.000和1.000。L1相对H0的同材界面退化曲线误差为0.156%，峰值误差为0.308%，近峰区重叠率为1.000。全部正式门槛通过，所有工况的质量标记均为非可疑，ODB实际时间步与2.0/1.0/0.5 ms目标值一致。

单点argmax诊断仍如实显示S0—M3和L1—H0分别偏移0.211h和0.640h并超过0.02h；该结果与第一版失败报告共同保留，不参与第二版总体判定。V3因此支持“当前控制性软层坡地工况下，8 m网格、1 ms时间步、6h侧向净空、5h底部深度及同材人工界面对表面放大曲线影响受控”的限定结论，不支持任意坡形、任意频率和任意材料组合均具有同一误差上界。
